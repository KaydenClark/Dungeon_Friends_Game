# Dungeon Friends - Taskboard

> Generated from LLM Workbench v2.1. See `RUNBOOK.md` -> Upgrading The
> Harness.

**Current focus: THE V2 VISION PIVOT (2026-07-11, D-024..D-035 in
`BLUEPRINT.md`).** Kayden's research pass triggered a controlled reboot of
the design: one persistent world, one shared environmental vocabulary, a
visible party of Dungeon Friends, and encounters that permanently resolve
problems - deterministic, telegraphed, in-room combat replacing the separate
d10 arena mode; Steam-first commercial target. **T-088 (docs-only canon
reset) is done; T-089 (three-quarter visual spike), T-090 (unified in-room
encounter spike), and T-092 (deterministic intent prototype) are built and
awaiting Kayden's verdicts - D-027 hangs on his played read of T-092.** The pivot sequence T-089..T-095 replaces the old "Phase 5 - Party
System & Progression" plan, and T-069's windowed acceptance no longer gates
anything (superseded - the arena/transition layer it was accepting is
retired by D-025).

**Do not delete working v1 code before its v2 replacement exists and is
verified** - retirement is staged through the pivot sequence (see
`BLUEPRINT.md` -> V2 Systems -> Reuse, Rework, Retire).

**Owner:** Kayden
**Last updated:** 2026-07-11 (v2 canon reset: `BLUEPRINT.md`, `AGENTS.md`,
and this board rewritten to the pivot; pivot sequence added as
T-089..T-095)

This is the live work queue and proof ledger. Agents use it to decide what to
work on next. Keep strategy and long-term direction in `BLUEPRINT.md`; keep
commands and verification procedures in `RUNBOOK.md`.

## Executive Brief

- **THE PIVOT (2026-07-11):** the project rebooted its design as v2 - unified
  in-room deterministic encounters (D-025/D-026/D-027), persistent resolution
  (D-028), visible party (D-029), 3/4 perspective on the orthogonal grid
  (D-030), shared material/effect vocabulary (D-031), Steam-first (D-032),
  roster of ~10-12 friends (D-033). Canon reset (T-088) is complete; the
  pivot sequence T-089..T-095 is the plan. Everything below this bullet
  describes the v1 build still on disk - accurate as software status,
  superseded as direction.
- **Playable now:** the forest/tutorial-dungeon slice plus the Phase 4
  tactical-combat core, now with an animated armored Hero, wizard Buddy, and
  red-ooze enemies replacing the player/combat blocks and enemy triangles:
  touch a visible enemy to fight, controlling Hero plus the temporary Buddy
  test companion (D-013) with
  interleaved initiative, movement/attack highlights, and
  Attack/Ability/Item/Defend. LDtk EncounterData drives regular two-enemy
  fights into D-018's seven authored forest arena templates; contact zooms into
  combat and the HUD exposes turn order and HP/MP without covering the board.
- **Health:** green on Godot 4.7. Fresh 2026-07-11 consolidation checks:
  `--import` clean, `main.tscn` boots (`SceneManager ready.`), unit tests PASS
  (35 suites / 235 tests / 1,331 checks), slice smoke PASS 134/134, and windowed
  arena-gallery/combat-showcase screenshots render at 1280x720. The
  the new two-process saveload battery PASS (save 10/10, load 11/11). The
  intentional bad-wiring pressure-plate test emits one warning; no test
  failures.
- **Latest decisions:** teammate collision stays as combat balance (D-020);
  Item must name/select its consumable (D-021); manual jumping is benched
  behind a future traversal item (D-022); the first pressure-plate room now
  isolates plate + block + gate on one floor (D-023 clarification); prompts
  show keyboard keys until controller glyphs exist.
- **Phase status:** Phase 1 is accepted. Phase 2's T-078 implementation is
  complete; its final visual/readability judgment rides T-069. **Phase 3 is
  now fully built** (2026-07-10): the M3.1 data foundation plus the resumed
  M3.2/M3.3 save/load half - SaveData/SaveManager, MapRegistry, the forest
  save crystal, Continue/New Game boot prompt, checkpoint respawns + XP-floor
  penalty, pit falls, always-respawn enemies (D-009), registry warps, and
  the two-process acceptance battery. Phase 4's data, math, TurnManager/FSM,
  multi-unit arena, range selection, command set, LDtk/reward wiring,
  transition, HUD, acceptance battery, and authored-arena lane are built.
- **Next work:** T-089, T-090, and T-092 are consolidated on
  `codex/unified-world-consolidation` and awaiting Kayden's verdicts
  (2026-07-11):
  the three-quarter spike room (`docs/screenshots/t089-three-quarter-spike/`),
  the unified in-room encounter inside it (8 continuity assertions green;
  `docs/screenshots/t090-unified-encounter/`), and the deterministic
  intent-rounds prototype built to Kayden's same-day spec (rolling 3-verb
  plan, verb-only future telegraphs, 23 live assertions green;
  `docs/screenshots/t092-intent-prototype/`). **The key pending input is
  Kayden's played D-027 verdict on the intent prototype.** T-093 (reaction
  prototype - the pivot's riskiest assumption) is next after that. T-069's
  acceptance pass is superseded by the pivot; the old Phase 5 plan is
  retired.
- **Alternative evidence retained:** Sol's isolated height/readability and
  visible-party exploration spikes remain on the consolidation branch as
  focused test harnesses. They are useful for evaluating elevation metadata,
  one-cell choke recovery, follower non-occupancy, and leader switching, but
  they are not a third production direction and do not supersede T-089/T-092.
- **Branch state:** Phase 4 landed into `integration` 2026-07-09; the Phase 3
  save/load lane landed into `integration` 2026-07-10 from
  `feature/phase3-saveload`. `main` remains Kayden-only; syncing
  `integration` -> `main` is his call, normally after T-069.
- **Assets:** production batches remain tracked in
  `docs/assets/ASSET_PLAN.md`, with generated-image provenance in
  `docs/assets/IMAGE_PROMPTS.md`. T-053/T-055 are in progress: runtime
  character/enemy animation is wired; NPCs, environment/objects, effects,
  UI polish, directional movement, and distinct enemy variants remain.

## Pending Decisions

### V2 pivot round (2026-07-11) - one OPEN decision, one flagged review

| ID | Decision | Resolution | Owner |
|---|---|---|---|
| D-027 | **Combat turn structure:** intent rounds (enemies declare -> player sees all -> party acts in any order -> enemies resolve -> environment resolves) vs. alternating per-unit initiative with intents shown a turn early? | **OPEN - provisional lean: intent rounds.** Resolved by the T-092 prototype's played verdict; do not commit combat architecture to either model before then. Full framing in `BLUEPRINT.md` D-027 | Kayden (via T-092) |
| (D-008 review) | Does the defeat XP-penalty flow still make sense under D-028's finite XP economy (no respawn grind to earn it back)? | **Flagged, not yet designed.** Revisit during T-094 progression work; candidates: smaller penalty, money-loss once currency exists, or time-loss only | Kayden |

All other pivot decisions (D-024..D-035) were resolved 2026-07-11 and live in
`BLUEPRINT.md` -> Design Decisions.

The architecture/toolchain decisions were resolved in the original research
audit (2026-06-11); the 2026-07-05/06 creative-direction additions were folded
directly into `BLUEPRINT.md` - see Design Decisions there. **All Phase 2 open
decisions are now resolved (2026-07-06 round 3)** and folded into `BLUEPRINT.md`
(Design Decisions + Party And Combat Model + Core Logic):

| ID | Decision | Resolution | Owner |
|---|---|---|---|
| D-001 | What does the tutorial **shield** do? | (a) plain inventory item at Phase 2; real effect a Phase 3/S-001 question | Kayden |
| D-002 | LDtk entities all-in vs. code/LDtk hybrid? | **All-in on LDtk entities** - confirmed the importer's post-import entity path is the well-documented one (template + working example + class docs). Foundational task T-031 | Kayden |
| D-003 | Jump automatic or a button? | **Superseded by D-022:** manual jumping is benched until a future traversal item defines a coherent rule. | Kayden |
| D-019 | Keyboard/controller anchor? | **1:1 mapping remains:** E/A interact+confirm; Space/B reserved traversal action; Q/X cancel/back; F/Y character menu; Tab/Start broader menu; WASD/D-pad movement. Current prompts show keyboard keys only; T-079 later replaces them with controller glyphs as a coherent mode. | Kayden |
| D-020 | Should allies block each other in combat? | **Yes.** Allied cells are not pass-through; movement order and lane management are intended balance. Arena validation prevents unavoidable starting traps rather than disabling collision. | Kayden |
| D-021 | How readable must Phase 4 Item use be? | **The item is named/selected before it is consumed.** T-077 adds the minimum combat item surface; the full inventory/character menu remains Phase 5. | Kayden |
| D-022 | What happens to manual jumping? | **Bench it until a future Zelda-style traversal item.** Do not require or advertise the standing facing-hop, and do not add a forward+jump timing chord to the grid controls. | Kayden |
| D-023 | What is the first dungeon puzzle set? | **Vocabulary remains latching lever + momentary plate + pushable blocks, but teach one mechanism per room.** T-078's second room is only the ALTTP-style plate/block/gate lesson on one continuous pit-free floor; latching levers wait for a later dedicated room. | Kayden |
| D-004 | Phase 2 death handling? | **Restart from the beginning of the game**; richer respawn deferred to Phase 3 (rescopes T-029) | Kayden |
| D-005 | Overworld party representation? | **Single avatar**; multi-character control only in tactical combat. Supersedes snake-follow (Phase 5 revision). *(2026-07-08 sharpening: the combat model is BG3's turn-based mode; Fire Emblem is the visual reference only - see `BLUEPRINT.md` -> Party And Combat Model)* | Kayden |

### Phase 3 planning round (2026-07-07) - **RESOLVED same day (Kayden)**

Gap analysis sources: Gameplan.md §3.1/§6/§12/§15 (M3.1-M3.3; Gameplan was
retired 2026-07-08 - its content is absorbed into `BLUEPRINT.md`/`RUNBOOK.md`/
this board, so §-references below are historical), the research
audit's Resource-paradigm recommendation, `BLUEPRINT.md` Data Model, and the
current code. Resolutions folded into `BLUEPRINT.md` (Design Decisions,
2026-07-07 Phase 3 rows + Core Logic); agent-interpretation details flagged
there and on the task rows.

| ID | Decision | Resolution | Owner |
|---|---|---|---|
| D-006 | Save file format? | **JSON** (`user://saves/slot_N.json`). Kayden had no preference and delegated the pick; agent chose JSON: Gameplan §12's own MVP recommendation (human-readable/hand-editable), and `.tres` loaded from the writable `user://` dir can instantiate embedded scripts - JSON stays pure data. `.tres` remains the format for authored data under `res://` | Kayden (delegated to agent) |
| D-007 | Shield's real effect? | **The shield unlocks the Defend command**: Defend is unavailable in the combat menu until `shield` is in the inventory, then usable. (Kayden: "lets take away the defend ability until you get the shield... I am sure we can make use of it during combat.") Rescopes T-046 | Kayden |
| D-008 | Respawn details (D-004 Phase 3 half)? | **Checkpoints, keep inventory, lose XP - "not having to do things over again" is never the punishment.** (1) Keep inventory on defeat. (2) Lose XP, but **never below the current level's floor** (money-loss may join/replace this once currency exists). (3) Defeat in a dungeon -> respawn at the **dungeon entrance**; outside -> the healer (2026-07-06 spec stands). (4) **Pit falls become real** (supersedes "pits are impassable"): walking into a pit = Zelda-style fall - small damage, respawn at the last entrance you came through into that room. Defeat never touches save files. Spawns T-047; rescopes T-041 | Kayden |
| D-009 | Do defeated enemies stay dead? | **No - "enemies respawn every time you leave the room."** The room-reset softlock escape valve applies to enemies too ("Crap I can't do this anymore" -> "that's okay, I'll just leave and come back"). Uniques/bosses included; duplicate key drops are already prevented by loot dedup. `SaveData.defeated_enemy_ids` is dropped from the schema. Deliberate deviation from Gameplan §12's Lufia II pattern. Spawns T-048 | Kayden |
| D-010 | EncounterData/MapMeta now or Phase 4? | **Now, as stubs** (classes + one sample .tres each, wired at Phase 4) - "based on 009 I say yes to this now." Unblocks T-044 | Kayden |
| D-011 | Boot flow with an existing save? | **Minimal Continue/New Game prompt** when a save exists; no title screen until Phase 6. Kayden's emphasis for current content is checkpoints/respawns over save-scumming - plus **expanded dev warps: "something that lets me go to anywhere we have built"** (spawns T-049, rides the T-038 registry). Unblocks T-040 | Kayden |

### Phase 4 planning round (2026-07-08) - **RESOLVED same day (Kayden)**

Gap analysis sources: `BLUEPRINT.md` (Party And Combat Model, Core Logic
combat rule, the 2026-07-08 tactics-RPG identity row), the current combat
walking skeleton (`game/scripts/combat/combat.gd` - 1v1, menu-driven,
placeholder formulas), and the retired Gameplan's M4 milestones (absorbed).

| ID | Decision | Resolution | Owner |
|---|---|---|---|
| D-012 | **Battle arena source**: local overworld terrain around the contact point, or a dedicated per-region arena scene? | **Local terrain** - Kayden: "use the local terrain where you were touched." The combat grid copies the room's blocked/pit cells in a window around the contact point ("they entered the same zone and spotted you"); open-field fallback if the local area is too cramped to place both parties. Rescopes T-062. **Reaffirmed 2026-07-09.** | Kayden |
| D-013 | **Phase 4 party size**: roster has only the hero until Phase 5 - ship combat 1-unit, or prove multi-unit control now? | **Test companion** - Kayden: "give me a test companion for now." Build for up to 4; ship Phase 4 with one clearly-placeholder companion in the party so initiative interleaving and unit selection are actually exercised; the first real Phase 5 recruit replaces it. Rescopes T-062. **Reaffirmed 2026-07-09.** | Kayden |

### Phase 3 tuning round (2026-07-10) - **RESOLVED same day (Kayden)**

Kayden's answers to the save/load lane's flagged interpretations, given
ahead of his PC playtest. The numeric ones are implemented; the loot one
spawns T-070/T-071.

| ID | Decision | Resolution | Owner |
|---|---|---|---|
| D-014 | Defeat XP penalty amount? | **Lose 25% of above-floor progress** - "some % of how much you have left to lose so it never feels too harsh... we can make it feel worse if we need to later." Replaces the lose-it-all first cut (`Progression.DEFEAT_XP_LOSS = 0.25`) | Kayden |
| D-015 | Respawn HP after defeat? | **80% of max HP** - "like in Zelda when you die you come back with 3 hearts, but here it's more like 80% health so you just need to eat a meal again to get back to full." Becomes less impactful once health potions/food exist (`SceneManager.RESPAWN_HP_FRACTION = 0.8`; MP still restores in full - agent interpretation, no MP economy yet) | Kayden |
| D-016 | Pit fall damage? | ~~10% of max HP~~ **Superseded by the 2026-07-10 windowed pass: flat 10 HP party-wide**; respawn-at-entrance stands (`Player.FALL_DAMAGE = 10`). The single overworld avatar represents the traveling party, so Buddy no longer enters the next battle untouched. | Kayden |
| D-017 | Where do consumables come from? | **All sources**: enemy loot collected as an **end-of-battle total** (the group-reward model already works this way - one summed banner per encounter); food/potions/treasure from chests; shops later. Mini-bosses drop their key plus potions - implemented now (boss slime: forest_key + 2 potions; key guardian: dungeon_key + 1 potion). Regular-enemy food/potion drops need a drop-chance model first (spawns T-070); shops/treasure ride the future currency system (spawns T-071) | Kayden |
| D-018 | How should battle location affect arena selection without copying unplayable overworld geometry? | **Authored, biome-consistent weighted shuffle bag.** D-012 is reinterpreted as biome/context fidelity, not literal nearby-cell copying. Start with seven editable LDtk arena templates: 2 `empty` (mostly open, at most 1-2 standalone trees), 3 `mid` (meaningful cover with generous routes), and 2 `hard` (scary, distinct terrain without trapping either side). Initial per-template bag weights: empty `5`, mid `2`, hard `1`, producing an approximate 56% / 33% / 11% tier mix. Draw without immediate repeats; refill when exhausted. Encounter biome/tags restrict the eligible pool, contact direction controls deployment orientation, and enemy count/strength varies independently. | Kayden |

## How To Use This Board

1. Read `BLUEPRINT.md` for context.
2. Pick the highest-priority `ready` task that is in scope and unclaimed.
3. Move it to `claimed` or `in-progress` before editing.
4. Do the smallest correct change.
5. Run the task's required proof and the relevant `RUNBOOK.md` checks.
6. Move the task to `done`, `blocked`, `deferred`, or `needs-review`.
7. Append one proof row with the actual result.

Do not rewrite existing proof rows. Append only.

## Status Values

| Status | Meaning |
|---|---|
| `ready` | Clear enough for the next agent to start. |
| `claimed` | An agent has picked it but has not edited yet. |
| `in-progress` | Work is underway. |
| `gated` | Implementation is done and waiting on verification, review, or merge. |
| `needs-review` | Needs human or manager review before more work. |
| `blocked` | Cannot proceed until the blocker is resolved. |
| `deferred` | Valid work, intentionally not next. |
| `done` | Proof exists and docs impact is resolved. |

A `claimed` or `in-progress` task that has gone stale (no update past one
working day) may be reclaimed per the reclaim rule in `AGENTS.md` -> Long
Session Control.

## Ready

**The v2 pivot sequence is the active plan** (2026-07-11, D-024..D-035;
`BLUEPRINT.md` -> V2 Systems). Execution order: T-089 -> T-090 -> T-092 ->
T-093 -> T-091 -> T-094 -> T-095 (IDs are not chronological - T-091's number
was assigned before the sequence order settled; follow the Depends column).
Spike tasks may live in `scenes/dev`/`scripts/dev` as throwaway code -
graduating a spike into production architecture is its own explicit decision.
**Do not delete working v1 code before its v2 replacement exists and is
verified.**

**The testing bar is unchanged:** pure/deterministic logic (damage math,
material reactions, intent resolution, elevation rules, persistence) gets
strict red/green per `RUNBOOK.md` -> Test Coverage Policy. Deterministic
combat makes MORE of the game pure-logic-testable, not less - the
preview-equals-result contract is itself a test.

### Pivot Sequence (v2)

| ID | Priority | Task | Why | Depends | Proof required | Owner | Status | Last update |
|---|---:|---|---|---|---|---|---|---|
| T-088 | 1 | **Pivot step 1 - canon reset (docs only).** Rewrite `BLUEPRINT.md` (v2 identity, pillars, non-goals, V2 Systems, D-024..D-035 superseding log), update `AGENTS.md` locked decisions, reset this board around the pivot sequence. No code changes. | Explicit supersession before any code is removed or rewritten - the review's recommended first step | - | The three docs read consistently v2; superseded decisions recorded by ID, not deleted | Claude | **done** (2026-07-11) | 2026-07-11 |
| T-089 | 1 | **Pivot step 2 - three-quarter visual spike.** One dev room on the orthogonal grid: two elevation levels via a small integer cell elevation, one ramp/stair transition, one tall vertical wall face, and four party members visible (directly controlled leader + 3 breadcrumb/formation followers). Kenney or recolored art is fine. **Explicitly out of scope:** high-ground bonuses, line-of-sight, combat. | Proves D-029/D-030 readability before any art, roster, or combat investment | T-088 | Windowed screenshots at 1280x720 + 1920x1080; Kayden's readability verdict (does height read? does the party read as a party?) | Claude | **needs-review** - spike built (`scenes/dev/three_quarter_spike.tscn`), both screenshot sets captured; Kayden's readability verdict remains | 2026-07-11 |
| T-090 | 1 | **Pivot step 3 - unified encounter spike.** Hero + one friend + one enemy fight directly inside the T-089 room: contact/detection starts the encounter in place, followers snap to nearby valid cells and become blocking tactical units (D-020 survives in-encounter), no scene change or zoom, and the overworld is simply continuous after victory. Reuse `TurnManager`/combat math where convenient - this spike does not decide the turn structure. | D-025's core claim - the same room is the battlefield | T-089 | Encounter starts, plays, and resolves in-room; world state (positions, puzzle state) continuous before/after; screenshot or short capture | Claude | **needs-review** - built (`scenes/dev/unified_encounter_spike.tscn`); scripted tour passes all 8 continuity assertions; Kayden play-check remains | 2026-07-11 |
| T-092 | 1 | **Pivot step 4 - deterministic intent prototype.** One enemy telegraphs its next action (intended movement, target cells, exact damage/status); the player can move out, block, stun, push, or obscure to change or cancel it; the shown preview always equals the result. Build the intent-rounds model (declare -> see -> party acts any order -> enemy resolves -> environment resolves) and compare in play against alternating initiative with early-shown intents. **This task decides D-027's turn structure - record the verdict as a Pending Decision resolution.** | D-026/D-027 are the combat identity; the turn-structure fork is the biggest architecture decision of the pivot and must be earned by play, not argued | T-090 | Red/green on preview=result and intent cancellation; a played comparison note (which model felt better and why); D-027 verdict recorded | Claude | **needs-review** - intent-rounds prototype built to Kayden's 2026-07-11 spec (rolling 3-verb plan, verb-only future telegraphs, full-detail current action); red/green + live assertions green; **Kayden's played verdict on D-027 remains the deliverable** | 2026-07-11 |
| T-093 | 1 | **Pivot step 5 - reaction prototype.** Material tags + effect propagation on the grid cells: grow a vine then burn it; flood a tile then freeze it; wet + spark conducts; air spreads fire or clears smoke - through **one code path used identically inside and outside the encounter**. | **The riskiest assumption of the whole pivot (D-031).** If this gray-box room is not fun, stop and rethink before scaling roster or world | T-092 | Red/green on the reaction matrix (each vocabulary row); the same verb function invoked from exploration and combat contexts; Kayden plays the room and records a fun/not-fun verdict | - | ready (after T-092) | 2026-07-11 |
| T-091 | 2 | **Pivot step 6 - persistence proof.** Defeat the enemy, alter the environment, leave the room, save, quit, relaunch, reload: the resolved encounter stays resolved and the environmental changes persist. `SaveData` gains resolved-encounter IDs and persistent environmental state (reverses D-009's schema rule per D-028). Also re-prove the soft-lock escape valve: leaving and re-entering still resets wedged puzzle blocks WITHOUT resurrecting resolved encounters. | D-028's contract, and the replacement for the protection D-009 used to provide | T-090, T-093 | Two-process battery in the T-042 style: resolve -> save -> quit -> relaunch -> assert resolved + environment persisted + block reset still works | - | ready (after T-093) | 2026-07-11 |
| T-094 | 2 | **Pivot step 7 - thesis slice.** One recruitable friend with a real verb, personality hook, and recruitment story (replaces D-013's Buddy placeholder contract per D-033); one NPC encounter with a non-combat resolution (D-034); one environmental puzzle using the shared vocabulary; one meaningful tactical fight. The v2 loop (adventure -> party progression -> encounter -> world change) end to end. | First integrated proof of the v2 game as a game | T-091 | Playable slice; each loop step demonstrable in under a minute each; Kayden acceptance | - | ready (after T-091) | 2026-07-11 |
| T-095 | 2 | **Pivot step 8 - external playtest.** Put the thesis slice in front of players who did not help design it, before scaling the roster or world. Capture first-session confusion, fun verdicts, and whether the verb system is discovered without prompting. | The Steam-first decision (D-032) makes external eyes a requirement, not a luxury | T-094 | Notes from 2+ external testers; re-cut tasks filed from findings | Kayden | ready (after T-094) | 2026-07-11 |

### Parallel Kenney-First Visual Skeleton Lane

*Pivot note (2026-07-11): this lane survives the v2 pivot - Kenney assets
remain the temporary visual scaffolding (V2 Systems -> Reuse). T-084's visual
review is still useful; the 3/4 perspective shift (D-030) lands later as an
art pass on top, starting with the T-089 spike.*

Kayden's 2026-07-11 direction supersedes the custom-generated cohesion lane:
use the checked-in CC0 Kenney library to skin the full playable framework now,
then swap in custom art later without changing mechanics. Preserve the source
library and current generated sprites; promote selected Kenney regions into
the established runtime paths and wire them through TileSets, SpriteFrames,
textures, and styles. Full plan: `docs/assets/KENNEY_IMPLEMENTATION_PLAN.md`.

**Asset order:** T-080 promotion manifest -> T-081 visible world skeleton ->
T-082 actors -> T-083 combat/UI/prompts -> T-084 cohesion QA. T-081 is the
first owner-visible milestone so the forest and dungeon stop reading as blank
geometry and colored stand-ins as early as possible.

| ID | Priority | Task | Source / why now | Touches | Proof required | Docs impact | Owner | Status | Last update |
|---|---:|---|---|---|---|---|---|---|---|
| T-080 | 1 | **Kenney promotion manifest + visual contract.** Select atlas regions for every visible forest, dungeon, actor, interactable, combat, UI, and prompt role; record pack/source region/runtime name/consumer/scale; build a 1280x720 contact sheet. No production-scene wiring yet. | Prevents ad-hoc pack mixing and makes later custom swaps mechanical. | runtime Kenney folders, manifest, preparation script, contact-sheet scene | source regions in bounds; names unique; 16x16/4x/nearest contract validated; contact sheet inspected | `docs/assets/KENNEY_IMPLEMENTATION_PLAN.md`, `docs/assets/ASSET_PLAN.md`; proof row | Codex | **done** | 2026-07-11 |
| T-081 | 1 | **Playable Kenney world skeleton.** Promote RPG Pack forest terrain plus Caves & Dungeons/Indoors dungeon tiles and interactables; update LDtk tilesets while preserving geometry, collision, entity ids, and puzzle semantics. Replace colored/blank stand-ins across the full forest-to-dungeon route. | This is the fastest path away from red squares and unreadable blank geometry. | runtime tiles/object atlases, LDtk projects/imports, tileset resources | LDtk schema/import green; room/puzzle tests + smoke green; five-room screenshot tour visually readable | plan + asset manifest + taskboard proof | Codex | **done** | 2026-07-11 |
| T-082 | 1 | **Kenney actor skeleton.** Promote distinct Hero, Buddy, healer, quest NPC, regular/dungeon slime, and boss sprites through existing SpriteFrames and stats resources. Keep generated knight/wizard/ooze files as unwired alternatives. | Makes the complete slice visually coherent while retaining clean later replacement seams. | runtime character atlas/sheets, SpriteFrames, CharacterStats/EnemyStats art refs | every role visible in overworld/combat showcase; no actor color blocks; units + smoke green; windowed scale/facing check | manifest + proof row | Codex | **done** | 2026-07-11 |
| T-083 | 2 | **Kenney combat UI + input prompts.** Apply Pixel UI framing and Input Prompts Pixel glyphs to boot/dialogue/combat/turn-order/targeting surfaces and implement T-079's active-input prompt mode without changing bindings or combat state. | Gives the skeleton a consistent readable interface and removes mixed-text prompt ambiguity. | UI textures/styles, prompt atlas/mode, existing UI scenes/scripts, tests | keyboard/controller modes each show one glyph set; combat readability screenshots; UI/input tests + smoke green | `RUNBOOK.md` only if controls/procedure change; proof row | Codex | **done** | 2026-07-11 |
| T-084 | 2 | **Kenney cohesion QA + swap-ready handoff.** Fix scale/filter/anchor/draw-order/palette/state-readability defects only; produce forest/dungeon/combat comparisons and a placeholder-coverage report; document exact later custom-art swap points. | Turns the packs into a dependable framework instead of a loose asset dump. | manifests, previews, targeted presentation fixes, asset docs | import/units/smoke/main boot green; three tours inspected; every visible placeholder replaced or justified; Kayden verdict | asset plan + taskboard proof | Kayden + Codex | **needs-review** - T-086 restores the owner-preferred slab/full-door visuals and documents the rejected Kenney crops; Kayden re-check remains | 2026-07-11 |
| T-085 | 1 | **Combat readability recut.** Give header text non-overlapping regions, render command choices as bounded rows, keep popups inside the field, and replace the side-bracket cursor with a full yellow destination-cell outline. | Kayden's T-069 screenshots show repeated text collision/clipping and several movement mis-selections. | combat view + UI regression tests + deterministic captures | red/green layout/cursor/popup checks at 1280x720 and 1920x1080; rendered menu/move/result captures inspected | `BLUEPRINT.md`, proof row | Codex | **done** - bounded header/command rows, replacement result text, clamped popups, and full-cell cursor rendered cleanly at both review sizes | 2026-07-11 |
| T-086 | 1 | **Restore accepted dungeon object silhouettes.** Revert blocks to the prior two-tone 52x52 slab and doors to the prior full-cell 56x56 keyed/steel presentation without changing puzzle behavior. | Kayden prefers the prior blocks; current Kenney crops read as a fence and half a door. | `PushableBlock`, `LockedDoor`, Kenney handoff docs, room captures | object bounds/state tests; hub/pit/fight screenshots inspected; smoke unchanged | asset plan + proof row | Codex | **done** - exact prior slab/full-door geometry restored; rejected crops documented as unwired | 2026-07-11 |
| T-087 | 1 | **Dungeon guardian encounter + authored dungeon arena.** Give FightRoom's guardian a dungeon-biome EncounterData and add one validated stone-hall LDtk arena so dungeon combat never falls back to forest. | FightRoom currently has no EncounterId, so arena selection defaults to `forest`; the registry contains only forest templates. | encounter resource, FightRoom LDtk entity, battle arena LDtk/data/registry, tests | red/green encounter/biome isolation; arena validator; rendered dungeon-combat capture | `BLUEPRINT.md`, `RUNBOOK.md`, proof row | Codex | **done** - FightRoom resolves `dungeon_guardian`; `dungeon_stone_hall` imports, validates, and renders stone terrain | 2026-07-11 |

| ID | Priority | Task | Source / why now | Touches | Proof required | Docs impact | Owner | Status | Last update |
|---|---:|---|---|---|---|---|---|---|---|
| T-020 | 1 | **M1.4**: re-validate the `canvas_items`/`expand` HD/ultrawide stretch settings with the **real** M1.1 tileset at 1280x720, 1920x1080, and 3440x1440. *Prep done 2026-07-06: the spike scene now draws the real 16x16 tiles at 4x (all 5 tiles + hero on screen, ColorRect fallback removed from the happy path); headless run confirms `real_art=true`.* **Remaining: Kayden's windowed run at the 3 resolutions** - confirm the grid reads undistorted and 21:9 shows an acceptable amount of world | retired Gameplan §15 Phase 1 (M1.4, now owned by `BLUEPRINT.md`); T-003 done | `game/scripts/dev/display_scaling_spike.gd` (done), windowed judgment (Kayden) | Windowed run at all 3 resolutions shows an undistorted grid; if `expand` reads poorly at 21:9, note the `keep`+letterbox fallback | `RUNBOOK.md` display-spike expectation; `BLUEPRINT.md` ultrawide-risk row if fallback chosen | Kayden (windowed) | **done** - Kayden's windowed check passed 2026-07-08 ("I have checked the game and it works fine"); `expand` confirmed, no letterbox fallback needed | 2026-07-08 |
| T-032 | 2 | **Phase 2 windowed acceptance (Kayden), round 2**: round 1 (2026-07-07) produced the B-05/B-06 findings and the 4-room rescope, all built as T-033. Now re-play the reworked loop: forest (trees visible? enough obstacles in the open stretch?) -> boss door -> hub lock-in -> find the loose brick in the wall -> ledge jumps -> chasm block-fill -> key fight -> west loop back -> north locked door -> chest/shield -> exit; try to wedge the brick (lever + fixed bricks should make it impossible); try a defeat; try the F1 dev overlay. Judge the remaining flagged interpretations and the jump binding (does Alt read OK on macOS, or should C lead?) | The Phase 2 "done" condition is Kayden's playthrough, not just the 109/109 smoke run | none (play + feedback) | Kayden's verdict recorded in a proof row; any re-cuts become new tasks | `TASKBOARD.md` proof row; `BLUEPRINT.md` if an interpretation is overturned | Kayden (windowed) | **done** - round 2 passed 2026-07-08: game plays fine, **Phase 2 accepted**; one feel note ("block movement is a bit chunky") parked as T-059; combat named the main complaint -> Phase 4 pulled forward | 2026-07-08 |
| T-036 | 1 | **M3.1/§3.1: `GameState` Resource refactor.** DONE 2026-07-07 - see Done lane + Proof Log | Everything else in Phase 3 hangs off this | `game/scripts/data/game_state.gd` (new), `scene_manager.gd`, `chest.gd`, `debug_overlay.gd`, tests | done | `BLUEPRINT.md` Architecture note added | - | **done** | 2026-07-07 |
| T-060 | 1 | **M4.1: combat math core - the real d10 formulas.** Replace the placeholder `hit_threshold`/`needed_roll`/`attack_damage` statics with the real stat-derived d10 percentage system (roll 1-10 vs a threshold; thresholds read as clean percentages per the locked Core Logic rule). Pure logic, extracted to `combat_math.gd` statics so combat and tests share one path. All numbers are first-cut tunables clearly marked for Kayden's playtest (T-069) | `BLUEPRINT.md` Core Logic: "exact threshold/damage formula is TBD at the Phase 3/4 combat-math implementation" - this is that implementation | `game/scripts/combat/combat_math.gd` (new, extracted from `combat.gd`), `test_combat_math` | **strict red/green**: failing suite first - threshold derivation from atk/def/speed, damage math, clamps (never <1 dmg, threshold bounds), % readability (threshold 7 = 70%) | `BLUEPRINT.md` Core Logic combat rule gains the concrete formula | - | **done** (2026-07-08, red/green; kept the playtested 5+atk-def band + added ability power/heal - numbers tunable at T-069) | 2026-07-08 |
| T-061 | 1 | **M4.2: `TurnManager` + two-layer FSM extraction.** Refactor `combat.gd`'s inline loop into the locked shape: Battle FSM (`Initialize -> CalculateInitiative -> UnitTurn (loop) -> EncounterEnd`) + per-entity FSM (`AwaitingTurn -> CheckRange -> Moving -> SelectingAction -> ExecutingCommand -> TakingDamage/Healing -> back or Dead`), with a `TurnManager` class sorting **all** combatants (party + enemy interleaved) by speed each round. Behavior-neutral first: the current 1v1 fight must play identically before anything new lands on the FSM | The locked two-layer-FSM/TurnManager decision, prerequisite for multi-unit control | `game/scripts/combat/` (new `turn_manager.gd` + state classes), `combat.gd`, tests | unit: initiative order incl. speed ties (stable/deterministic rule chosen + pinned), FSM transition table, dead-unit skip; smoke unchanged (behavior-neutral) | `BLUEPRINT.md` combat scene-contract row | after T-060 | **done** (2026-07-08, red/green `test_turn_manager`) | 2026-07-08 |
| T-062 | 2 | **M4.2: multi-unit parties + arena build.** Combat takes a party (roster from `GameState`, architecture for up to 4; D-013's temporary companion proves it) vs an enemy party built from `EncounterData.enemy_group` (T-044's stub becomes a real consumer); arena grid per D-012 (local-terrain seed recommended); deterministic unit placement on entry | `BLUEPRINT.md` Party And Combat Model: party encounters, party of up to four | combat scripts, `encounter_data.gd`, arena/placement code, tests, smoke | unit: party build from roster + encounter, placement collision-free; smoke: a 2v2 battle runs to victory | `BLUEPRINT.md` combat row; D-012/D-013 outcomes recorded | after T-061 + T-044; D-012/D-013 judged by then | **done** (2026-07-08 per Kayden's D-012/D-013 picks; arena additionally restricted to the contact-connected region so seeded battles can't stalemate) | 2026-07-08 |
| T-063 | 2 | **M4.3: unit selection + move/attack range highlighting.** On a unit's turn: compute its move range (new `move_range` field) via grid flood over the arena and its attack range (new `attack_range` on stats/abilities) - **this task decides the BLUEPRINT-flagged range-field shape** on `CharacterStats`/`EnemyStats`/`AbilityData`; render FE-style range highlights; cursor/WASD cell pick with confirm/cancel; movement stays grid-snapped Tween (locked invariant) | The Fire Emblem visual affordance (BLUEPRINT 2026-07-08 combat identity) + the flagged Phase 4 data decision | stat/ability classes + `.tres` files, combat scripts, highlight rendering, tests | **red/green** on range calc (blocked cells, min/max range, no-diagonal reach); windowed readability judged at T-069 | `BLUEPRINT.md` Data Model rows gain the range fields (closes the 2026-07-05 note) | after T-062 (+T-035/T-043 for the ability fields) | **done** (2026-07-08: move_range/attack_range on stats, attack_range per ability; flood-fill highlight + cursor + threat fringe; windowed readability is T-069's) | 2026-07-08 |
| T-064 | 2 | **M4.3: command set - Attack/Ability/Item/Defend.** Per selected unit: Attack (range-checked via T-063), Ability (consumes `AbilityData` + MP - `strike`/`mend` from T-035 become real), Item (consumables via T-034's `ItemData` - add a first `potion.tres`), Defend (damage reduction; **shield-gated per T-046/D-007**). Enemy AI MVP: move toward nearest target within move range, attack if in attack range - nothing smarter (flanking/terrain are Deferred) | The locked command set; T-034/T-035's data finally gets a consumer | combat scripts, `game/data/abilities/`, `game/data/items/potion.tres`, tests, smoke | **red/green** per command branch (MP spend + insufficient-MP refusal, item consume + qty decrement, defend reduction math); smoke: one battle exercises all four commands | `BLUEPRINT.md` combat row; `RUNBOOK.md` play loop | after T-063 + T-034/T-035 (+T-046 for the Defend gate) | **done** (2026-07-08; "Wait" added as the always-available turn-end - agent interpretation, flag if wrong; potions have no natural source yet beyond dev grants - flagged for T-066/T-069) | 2026-07-08 |
| T-065 | 3 | **M4.4: camera-zoom combat transition.** Replace the fade with the early-FF zoom into the contact point ("far enough that the enemy is no longer on top of you") and zoom back out on encounter end; rides the existing SceneManager context-passing pattern - explicitly not a rearchitecture (BLUEPRINT transition rule) | The 2026-07-05 framing decision, deferred from Phase 2 to "Phase 4 framing" | transition code in `scene_manager.gd`/`main.gd`/combat entry | headless: transition completes and restores exact position (existing smoke assertions keep passing); zoom *feel* judged at T-069 | `BLUEPRINT.md` transition row -> built | after T-062 | **done** (2026-07-09; 2.2x player-camera zoom before the hand-off fade and restore after combat) | 2026-07-09 |
| T-066 | 3 | **M4.4: encounter + reward wiring.** Overworld enemies carry an `EncounterData` ref (LDtk entity field, post-import + `LdtkRoom` adoption); combat builds the enemy party from `enemy_group`; victory XP flows through T-045's curve per defeated enemy; loot stays string ids through the T-034 item library (dedup kept) | Closes D-010's "stubs now, wired at Phase 4"; retires the direct-EnemyStats encounter path | `entities_post_import.gd`, `ldtk_room.gd`, encounter `.tres` files, `scene_manager.gd`, `.ldtk` levels, tests | unit: encounter -> enemy-party build + reward math; smoke: forest fights run through EncounterData | `BLUEPRINT.md` EncounterData/MapMeta rows -> wired | after T-062 + T-045 | **done** (2026-07-09; regular forest `EncounterId=forest_pair` loads two slimes and grants 10 XP) | 2026-07-09 |
| T-067 | 3 | **M4.4: combat HUD.** Per-unit HP (and MP) readouts, a turn-order strip fed by the TurnManager, damage/heal popups, clearer action prompts. Placeholder art; coordinates with Asset Batch E (T-055) which starts with Phase 4 | Readable-Tactical-Combat pillar: "clear stats, visible outcomes, a legible board" | combat UI scripts (`CanvasLayer`) | unit: turn-order strip matches TurnManager order incl. deaths; readability judged at T-069 | `BLUEPRINT.md` UI scene-contract row | after T-063/T-064 | **done** (2026-07-09; order/death removal unit-tested and live 2v2 HUD visually checked) | 2026-07-09 |
| T-068 | 2 | **Phase 4 acceptance battery (headless).** Scripted battle(s) proving: initiative strictly per-unit by speed with mixed parties (never team-phase), movement/attacks refused outside range, all four commands including MP/item bookkeeping, victory -> rewards -> exact-position overworld restore, defeat -> the current defeat flow; smoke test extended; 5/5 consecutive runs (real-time-tolerant like Phase 2's) | The phase-done proof, same pattern as Phase 2's 109/109 battery | test suites + `slice_smoke_test.gd` | battery + full suite green 5/5; documented as the Phase 4 check | `RUNBOOK.md` Test And Build section | after T-060..T-066 | **done** (2026-07-09: 22 suites / 139 tests / 489 checks; smoke 111/111 on 5/5 including LDtk two-enemy encounter, 10-XP reward, zoom return, and defeat flow) | 2026-07-09 |
| T-069 | 2 | **Final Phase 4 + tutorial windowed acceptance (Kayden).** Play several encounters and the revised dungeon end to end: authored arena variety/readability, ally-blocking tactics, named Item selection, zoom/ranges/commands, keyboard-only prompts, lever/plate/block cause-effect, and no required jump | The phase "done" condition is Kayden's playthrough, not just the battery | final windowed pass after T-072..T-075/T-077/T-078 | verdict in a proof row; re-cuts become new tasks | `TASKBOARD.md` proof row; `BLUEPRINT.md` for accepted decisions | Kayden re-check | **superseded by the v2 pivot (2026-07-11, D-024/D-025)** - the separate arena/transition layer this pass was accepting is retired, so the acceptance gates nothing. The tutorial/puzzle and Kenney visual work it covered survive; T-084's visual review remains independently useful. | 2026-07-11 |
| T-072 | 1 | **Authored arena data + weighted shuffle bag (D-018).** Add an arena record/registry with stable id, biome/tags, tier (`empty`/`mid`/`hard`), weight, LDtk level id, mirror-safe flag, and party/enemy deployment zones. Implement a seeded weighted bag with per-template weights empty `5`, mid `2`, hard `1`; filter by biome/tags; prevent immediate repeats; refill deterministically. Selection state must survive save/load through a seed + draw counter or equivalent compact state so reload cannot reroll an easier board. | Kayden's second T-069 pass: retain contextual variety without procedural topology | new arena data/registry + pure selector + GameState/SaveData fields | strict red/green: tier distribution ticket counts, biome filtering, no immediate repeat, deterministic refill, save/load continuation | `BLUEPRINT.md` arena contract; `RUNBOOK.md` debug controls | Codex | **done** - seven records; deterministic selector state persists through JSON save/load | 2026-07-11 |
| T-073 | 1 | **Author the first seven editable LDtk battle arenas.** Forest MVP pool: 2 `empty` templates (mostly empty, 0-2 standalone trees), 3 `mid` templates (cover with multiple wide routes), 2 `hard` templates (visually scary/distinct but intentionally fair). Every template is a named LDtk level visible/editable by Kayden and imports to `TileMapLayer`; include explicit party/enemy deployment-zone entities. No deprecated `TileMap`. | Gives Kayden direct visual ownership and the requested 2/3/2 variety | LDtk project + imported levels + arena registry records | import clean; screenshot/contact sheet of all 7; validator passes every arena | `RUNBOOK.md` exact edit/import/preview steps | after T-072 schema | Codex | **done** - `battle_arenas.ldtk` has seven imported 17x7 levels with 8 slots per side | 2026-07-11 |
| T-074 | 1 | **Replace literal contact-terrain generation with authored arena selection.** `EncounterData` supplies biome/tags (and optional fixed arena for bosses); the selector draws an eligible template. Preserve the good part of D-012: current map/encounter context prevents biome nonsense, and the direction/side of overworld contact orients party versus enemy deployment. Enemy group count/strength remains independent from terrain tier, allowing the same arena to feel different across encounters. Remove `_arena_from_room` as the production arena source. | Fixes unfightable terrain while retaining where-you-met context | `EncounterData`, SceneManager combat entry, combat setup, LDtk arena loader | red/green integration: forest never selects non-forest; contact side flips deployment; fixed boss override; every group can path between zones | `BLUEPRINT.md`, `RUNBOOK.md` | after T-072/T-073 | Codex | **done** - live SceneManager selection/LDtk loader replaces `_arena_from_room` | 2026-07-11 |
| T-075 | 1 | **Arena safety validator + gallery preview.** Validate connected deployment zones, enough legal cells for up to 4v4, Hero starts with at least two legal movement directions, intentional ally collision cannot create an unavoidable deployment softlock, and no ordinary arena exceeds the agreed cover budget. Allies remain blocking after deployment (D-020). Add a one-command or F1 gallery that cycles all eligible arenas with ids/tier/weight visible so Kayden can inspect and edit them quickly. | Authored does not automatically mean playable; this is the proof and owner workflow | tests/dev scene/debug overlay + `RUNBOOK.md` | validator negative fixtures fail for the expected reasons; all seven production arenas pass; gallery screenshot/contact sheet | `RUNBOOK.md` authoring checklist | after T-073/T-074 | Codex | **done** - shared validator, gallery, live-combat showcase, and inspected capture | 2026-07-11 |
| T-077 | 1 | **Readable combat Item command (D-021).** Replace immediate generic Item use with the smallest named selection/confirmation surface: list usable consumables with display name and quantity, show which unit will use which item, refuse empty inventory clearly, then consume only the confirmed item. Do not build the full inventory/character menu here. | Latest playthrough: pressing Item gave no indication what would be used; this is a Phase 4 readability defect, not optional Phase 5 polish | combat command UI + ItemLibrary lookup + tests | strict red/green: item identity/qty shown, choice consumes the selected id only, cancel consumes nothing, empty state readable; windowed proof at T-069 | `BLUEPRINT.md` combat/UI contract; `RUNBOOK.md` controls | Codex | **done** - named quantity/actor choice, Back, empty state, and selected-id consumption shipped | 2026-07-10 |
| T-078 | 1 | **Phase 2 puzzle gap closure (D-022/D-023).** Bench manual jump in shipped rooms and teach the first pressure-plate puzzle on one unambiguous open floor: stepping on the plate opens the room's north gate, stepping off closes it, and pushing the heavy block onto it holds the gate open. No pits and no second mechanism compete with that lesson. Keep a reset/exit route so no block state can softlock the room. | Kayden's screenshot playtest found the pit bands visually ambiguous and the two-mechanism sequence confusing; ALTTP's single plate/block/gate room is the clarified reference | LDtk tutorial level, plate/block visual states, tests/smoke | red/green primitive tests; exhaustive block-state recovery check; smoke completes the room by holding the plate; under-one-minute demo route | `BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, `README.md` | Codex | **done** - continuous floor, one brass plate, one block, one north gate, reset lever; no pits/latch lesson | 2026-07-10 |
| T-070 | 3 | **D-017: regular-enemy consumable/food drops.** Design the drop-chance model first (today `loot_table` is deterministic - every kill drops everything, which with D-009 respawns would flood potions), then: a first food `ItemData` + `.tres`, chance-based potion/food entries for regular slimes, and the end-of-battle total-collection banner listing them (already the group-reward shape). Keys stay deterministic on their carriers | Kayden (D-017): "when we kill enemies they should drop loot... food, potions, treasure" | loot model (`EnemyStats`/reward code), `game/data/items/`, enemy `.tres`, tests, smoke | unit: drop-chance model red/green (seeded RNG), key drops stay guaranteed; smoke: a fight yields its rolled loot in one banner | `BLUEPRINT.md` Data Model loot note; `RUNBOOK.md` play loop | after T-069 (judge the mini-boss potion feel first) | ready | 2026-07-10 |
| T-034 | 1 | **M3.1: `ItemData` + the item library + inventory becomes `{item_id: qty}`.** `ItemData` Resource (id, display_name, description, item_type enum KEY_ITEM/CONSUMABLE/EQUIPMENT, stat_modifiers, on_use_ability - stretch fields present but unused) + `game/data/items/` .tres for forest_key, dungeon_key, shield + an item library helper (id -> ItemData lookup). GameState.inventory becomes `{id: qty}`; doors/chests keep matching on string ids; HUD/victory banner/dialogue read `display_name` from ItemData instead of `id.capitalize()` | M3.1; D-007's shield effect and Phase 4 consumables both need real item records | `game/scripts/data/item_data.gd`, `game/data/items/*.tres`, `game_state.gd`, `chest.gd`, `locked_door.gd`, `debug_overlay.gd`, HUD, `scene_manager.gd`, tests | red/green: new `test_item_data` suite (library lookup, qty add/remove, key dedup) written failing-first; all suites + smoke green | `BLUEPRINT.md` Data Model row -> "built" | after T-036 | **done** (2026-07-08, first Phase 4-lane task - see Done lane + Proof Log) | 2026-07-08 |
| T-037 | 1 | **M3.2: `SaveData` + save manager.** `SaveData` shape per the retired Gameplan §6 (now the `BLUEPRINT.md` Data Model SaveData row) minus `defeated_enemy_ids` (dropped - D-009 says enemies always respawn): `schema_version`, `current_map` (id, see T-038), `player_position: Vector2i`, GameState payload (roster/levels/xp/hp, inventory, flags). Serialize to **JSON** `user://saves/slot_N.json` (D-006 resolved), 3 slots, atomic write (temp+rename), tolerant load (missing/corrupt file -> null + warning, never a crash). Owned by SceneManager (single-autoload lock - a helper class, not a new autoload) | M3.2 core; persistence rides on it | `game/scripts/data/save_data.gd` + save manager helper, `scene_manager.gd`, tests | red/green: round-trip suite (save -> wipe GameState -> load -> deep-equal), corrupt-file and missing-file cases, slot isolation - written failing-first | `RUNBOOK.md` (save file location, how to wipe saves, troubleshooting row) | after T-036 | **done** (2026-07-10, red/green `test_save_manager` 37 checks: round-trip, atomic temp+rename, corrupt/missing -> null+warning, slot isolation, JSON-float re-inting) | 2026-07-10 |
| T-038 | 2 | **Map registry.** Stable map ids (`forest`, `tutorial_hub`, `tutorial_chest`, `tutorial_pit`, `tutorial_fight`) -> room-factory Callables in one place; `SaveData.current_map` stores the id; `restart_game()`/load flow/debug-overlay warps all resolve through it (today each hardcodes constructors) | Load flow needs to rebuild a room from serialized data; three call sites already duplicate the knowledge | `scene_manager.gd` or a small registry script, `main.gd`, `debug_overlay.gd`, room scripts | unit: every registered id builds its room headless; overlay warps still work | `BLUEPRINT.md` scene contracts note | after T-036 | **done** (2026-07-10, `test_map_registry`: all 5 ids build headless, `id_for` reverses live rooms for SaveData; boot/restart/load/warps all resolve through it) | 2026-07-10 |
| T-039 | 2 | **M3.2: SaveCrystal entity.** New LDtk entity type (post-import hook + `LdtkRoom` adoption + entity-contract doc line), interactable: confirm dialogue -> writes slot 1 (multi-slot UI is not MVP; slots exist in the format per D-006). Placed next to the healer's campfire in `forest.ldtk` ("one save point object in the test area", M3.2). Saving inside the dungeon: not at MVP - crystals only exist in the forest, sidestepping mid-puzzle-save edge cases (the retired Gameplan §12's no-mid-dungeon-autosave rationale) | Save points are physical objects (retired Gameplan §12, now the `BLUEPRINT.md` Save rule) | `entities_post_import.gd`, `ldtk_room.gd`, `game/scripts/puzzles/save_crystal.gd` (new), `forest.ldtk`, smoke test | smoke: interact -> slot file exists with current position/flags; screenshot for the proof row | `RUNBOOK.md` play-loop line | after T-037 | **done** (2026-07-10; crystal at (21,14) beside the campfire, smoke leg proves slot 1 carries position/flags/inventory; *flagged interpretation:* the dialogue CONFIRMS the save after writing - no choice UI exists pre-Phase 6; windowed look folds into T-069) | 2026-07-10 |
| T-040 | 2 | **M3.2: load flow + boot prompt.** Boot with an existing save -> minimal Continue/New Game prompt (D-011 resolved) -> SceneManager loads SaveData, boots `current_map` via the registry, places the player at `player_position`, GameState restored; rooms already rebuild opened-doors/chests from flags on `_ready()`, so no per-object load code should be needed (verify, don't assume) | The M3.2 done condition is the full cycle, and a save you can't get back into isn't a save | `main.gd`, `scene_manager.gd`, maybe a tiny boot-prompt UI | headless: write save -> fresh process -> load -> assert position + a door flag + inventory survive | `RUNBOOK.md` boot flow | after T-037/T-038 | **done** (2026-07-10, red/green `test_load_flow` + the T-042 two-process battery; verified rooms DO rebuild doors/chests from flags on `_ready()` - no per-object load code needed; BootPrompt: E/Space Continue, X/Esc New Game) | 2026-07-10 |
| T-041 | 2 | **D-004/D-008: checkpoint respawn + XP penalty.** Defeat in a dungeon -> respawn at the **dungeon entrance** (hub spawn), rooms between freed (puzzle + enemy reset via T-048); defeat outside -> respawn at the healer's campfire. **Keep inventory; lose XP but never below the current level's floor** (uses T-045's curve; exact penalty is a tunable - first cut: lose progress toward the next level, flag for playtest). Full HP on respawn (agent interpretation - defeat already costs XP; flag if wrong). Defeat never touches save files. Replaces restart-from-the-beginning; `restart_game()` stays for the dev overlay | Kayden 2026-07-07: "I want loosing XP... to be the punishment, not having to do things over again" | `scene_manager.gd` (`handle_defeat`), respawn anchors via T-038 registry, tests, smoke defeat leg | unit: both respawn branches + XP-floor clamp red/green; smoke: defeat inside the dungeon lands at the hub entrance with wall reset, inventory intact, XP clamped | `BLUEPRINT.md` Death & respawn rule rewritten | after T-036/T-038/T-045 (+T-048 for enemy reset) | **done** (2026-07-10, red/green `test_defeat_respawn` + `xp_after_defeat` curve tests + both smoke defeat legs; same-day tuning per Kayden D-014/D-015: XP penalty 1.0 -> 0.25 of above-floor progress, respawn HP full -> 80% of max) | 2026-07-10 |
| T-047 | 2 | **Pit falls (D-008 part 4) - supersedes "pits are impassable" (2026-07-06).** Walking into a pit cell = Zelda-style fall: brief drop animation, small damage (first cut: 1 HP, tunable), respawn at the **last entrance you came through into that room** (track the entry cell on every doorway transition/spawn; jumping is unchanged and never falls). Fall damage reaching 0 HP -> the T-041 defeat flow. Enemies/pathing still treat pits as blocked; blocks still sink and fill | Kayden: "if you just fall into the pit instead of run out of HP you just walk back through the last entrance you came through. kinda like how zelda does" | `player.gd` (`try_step` pit branch), `ldtk_room.gd`/`room_grid.gd` (entry-cell tracking), pit room, `test_jump`/new suite, smoke | unit: fall -> damage + entry-cell respawn, jump still crosses, fall-to-0 chains to defeat; smoke: pit-room fall leg | `BLUEPRINT.md` Core Logic pits bullet superseded; `RUNBOOK.md` play loop | after T-041 | **done** (2026-07-10, red/green `test_pit_fall` + smoke ledge-fall leg; `entry_cell` stamped at spawn + on every room restore; enemies/pathing still treat pits as solid; same-day tuning per Kayden D-016: flat 1 HP -> 10% of max HP, floored at 1) | 2026-07-10 |
| T-048 | 2 | **Enemy respawn rule (D-009): enemies respawn every time a room is left-and-rebuilt.** Remove the `defeated_<unique_id>` respawn suppression in `LdtkRoom._adopt_entities` (uniques and bosses respawn too; loot dedup already prevents duplicate keys); keep door/chest flag persistence untouched. Suspended-and-restored rooms (mid-trip, e.g. the forest while you're in the dungeon) keep their in-visit state - "leaving the room" means the freed/rebuilt path, same as the puzzle escape valve (agent interpretation of Kayden's rationale; flag if wrong). Adjust the "boss stays dead" smoke/unit expectations; `defeated_` flags stay only as dialogue markers or get dropped | Kayden: "enemies respawn every time you leave the room... This should be a good way to keep us from shooting ourselves in the foot" | `ldtk_room.gd`, `tutorial_fight_room.gd` (quiet-chamber line), tests, smoke | unit: rebuilt room respawns its unique enemy, key not duplicated on re-kill; smoke updated | `BLUEPRINT.md` D-009 row + Core Logic (deviation from the retired Gameplan §12's Lufia-II pattern already noted there) | after T-036 | **done** (2026-07-10, red/green `test_unique_enemies_respawn_on_rebuild`; `defeated_*` flags dropped entirely - the dead quiet-chamber dialogue branch went with them; suspended-room in-visit state kept, pinned by the existing smoke check) | 2026-07-10 |
| T-049 | 3 | **Dev warp menu: "go to anywhere we have built."** F1 overlay warp list built from the T-038 map registry instead of the hardcoded 4 rooms - every registered room (forest, hub, chest, pit, fight, and anything future) gets a warp entry automatically; grants stay as-is | Kayden 2026-07-07: "warp tools would be nice. something that lets me go to anywhere we have built" | `debug_overlay.gd` | unit: warp list matches registry; windowed F1 pass is Kayden's | `RUNBOOK.md` dev-tools note | after T-038 | **done** (2026-07-10; keys 1-5 = registry order with labels, reset moved 5 -> R, grants unchanged; warns loudly if the registry outgrows the 5 warp keys; windowed F1 pass folds into T-069) | 2026-07-10 |
| T-042 | 1 | **M3.2/M3.3 acceptance battery.** Scripted two-process headless proof: run 1 plays to the save crystal (opened door + item in inventory), saves, exits; run 2 boots, loads, asserts position/flags/inventory match - the literal "save -> quit -> relaunch -> load" M3.2 done condition. Plus M3.3: one puzzle's solved state (tutorial chest/entry door) persisting across the cycle, and a smoke-test save/load leg | The phase's stated done conditions, as one command an agent can rerun | new dev scene or shell driver + `slice_smoke_test.gd` | the battery exits 0; RUNBOOK documents it as the Phase 3 check | `RUNBOOK.md` Test And Build section | after T-037..T-040 | **done** (2026-07-10; `scenes/dev/saveload_battery.tscn` two-process proof: save phase 10/10 via real chest/door/crystal interacts, load phase 11/11 incl. answering the boot prompt with a real confirm event and the chest's solved state surviving quit+relaunch; smoke gained an in-process load leg) | 2026-07-10 |
| T-035 | 3 | **M3.1: `AbilityData`** Resource (id, display_name, mp_cost, target_type, power, element/overworld_use stretch fields) + 2 sample .tres (`strike`, `mend`); `CharacterStats.starting_abilities`/`EnemyStats.abilities` become `Array[AbilityData]`. Data only - combat consumes nothing until Phase 4's command menu. *Note (2026-07-08): the tactics-RPG reframe means AbilityData will eventually carry move/attack-range fields (`BLUEPRINT.md` Data Model note); their shape is a Phase 4 decision - don't invent them here, just don't preclude them* | M3.1; Phase 4's Attack/Ability/Item/Defend menu needs the records to exist | `game/scripts/data/ability_data.gd`, `game/data/abilities/*.tres`, stat classes, `test_data_resources` | .tres instances load + fields assert in `test_data_resources` | `BLUEPRINT.md` Data Model row -> "built (stub)" | - | **done** (2026-07-08, abilities consumed by T-064 same day) | 2026-07-08 |
| T-043 | 3 | **Stat-class spec alignment**: portrait/combat-sprite `@export`s on CharacterStats/EnemyStats (empty until the art pass), typed ability arrays (with T-035). Deliberate deviation to flag: `EnemyStats.loot_table` stays **string ids** resolved through the T-034 item library, not the retired Gameplan §6's `Array[ItemData]` - string ids are what LDtk entity fields and victory-reward code already speak, and the library gives the same type safety at one indirection. *Note (2026-07-08): CharacterStats/EnemyStats will also eventually grow move/attack-range fields for the tactics combat (`BLUEPRINT.md` Data Model note) - Phase 4 decides the shape; don't preclude* | M3.1 alignment without complicating the LDtk field pipeline | `character_stats.gd`, `enemy_stats.gd`, `.tres` files, `test_data_resources` | suites PASS; `--import` clean | `BLUEPRINT.md` Data Model notes the loot_table deviation | after T-034/T-035 | **done** (2026-07-08, incl. the T-063 move_range/attack_range fields) | 2026-07-08 |
| T-044 | 3 | **`EncounterData` + `MapMeta` data shapes** (D-010): classes + one sample `.tres` each. Combat consumes `EncounterData.enemy_group`; T-066 completed the LDtk instance/reference and reward wiring. `MapMeta.music_track` remains a later content hook | Completes M3.1's class list and gives Phase 4 a typed encounter party | `game/scripts/data/`, `game/data/encounters/`, `test_data_resources` | `.tres` load headless | `BLUEPRINT.md` Data Model status | - | **done** (2026-07-08; integrated 2026-07-09 via T-066) | 2026-07-09 |
| T-045 | 3 | **XP/level data shape** (not mechanics): `party_levels`/`party_xp` live on GameState/SaveData from T-036; add a pure `xp_to_next(level)` curve function as tested logic (placeholder numbers, clearly marked). Level-up *mechanics* (stat growth on level, combat integration) stay Phase 5 (M5.3) - do not build them early | SaveData must carry progression from day one or every save schema-bumps at Phase 5; the curve is the first strict red/green candidate | `game_state.gd` or `progression.gd`, tests | red/green: curve suite written failing-first (monotonic, positive deltas, level-1 base) | `BLUEPRINT.md` progression note | after T-036 | **done** (2026-07-08, red/green `test_progression`) | 2026-07-08 |
| T-046 | 3 | **Shield unlocks Defend (D-007 resolved).** The combat menu's Defend command is absent/greyed until `shield` is in the inventory, then selectable (Defend's damage-reduction behavior itself unchanged for now). Chest reward + shield pickup dialogue say so ("You can now Defend in combat!"); smoke test asserts the gate both ways. Closes the D-001 debt | Kayden: "lets take away the defend ability until you get the shield, and then you can use it" | `combat.gd` (menu build + defend branch), chest/dialogue copy, `test_combat_math`/menu test, smoke | unit: defend absent without shield, present with it; smoke: pre-shield fight lacks Defend, post-shield fight has it | `BLUEPRINT.md` D-001 row resolved; combat scene contract note | after T-034 | **done** (2026-07-08: Defend absent from the menu without the shield, present with it - `test_combat_scene` pins both ways; chest copy announces the unlock) | 2026-07-08 |

## Backlog

Valid, scoped work that is not a stretch goal but is intentionally not next.
Distinct from Deferred (the stretch goals - from the retired Gameplan §17,
now sequenced in `BLUEPRINT.md`'s build order - gated behind the whole MVP).

*Pivot note (2026-07-11): every pre-pivot row below needs re-triage against
the v2 canon before being picked up. Known impacts: T-070 (drop-chance loot
model) was designed around D-009 respawns - under D-028's finite encounters
the drop economy changes shape entirely; T-002 (Android export) sits behind
D-032's mobile postponement; T-076 (character menu) should be designed
against the v2 party/verb model, not the v1 roster.*

| ID | Priority | Task | Why deprioritized | Revisit trigger | Status |
|---|---:|---|---|---|---|
| T-002 | low | M0.3: set up export presets for macOS, Windows, Android (debug); produce one trivial build per platform (`game/export_presets.cfg`) | The only remaining Phase 0 item, but Kayden's 2026-07-05 call is that per-platform distribution is premature while the game is still placeholder art - it doesn't block any Phase 1 work. Proof when done: one exported build per platform runs and shows the current scene | Once Phase 1 real-art/LDtk pipeline (T-003/T-004/T-011) has produced something worth building for a device, or Kayden wants an Android smoke test | deferred |
| T-028 | low | Enemy aggro telegraph: when an ooze/slime spots the player (crosses the notice radius and switches from wander to chase), show it - visibly "angry" (sprite/animation change), faster step cadence, and/or another clear cue. The player should always know they've been spotted | Kayden 2026-07-06 (his real meaning behind the movement table's "attack lock-in"): "the oozes being angry and getting faster and some other visible cue to let me know I've been spotted" - explicitly "a task for later"; needs real sprites to telegraph with | After T-003 lands real enemy sprites (a speed-up alone could ship sooner if wanted) | deferred |
| T-051 | medium | Asset Batch A: forest overworld tile/object pass - grass/path/tree/shrub/rock/dungeon entrance/campfire/sign variants per `docs/assets/ASSET_PLAN.md`. Started 2026-07-08 by Kayden request with a reproducible first-pass sheet at `game/assets/art/tilesets/forest_overworld_tiles.png`; runtime LDtk wiring remains gated | Placeholder art was intentional through Phase 2; starting source art is OK by request, but replacing runtime tiles should still wait until Kayden accepts the reworked tutorial dungeon so the map target is stable | After T-032 round-2 acceptance, before judging the slice visually or doing T-020's final real-art display pass | in-progress |
| T-052 | medium | Asset Batch B: tutorial dungeon tile/object pass - floor/wall/fixed brick/loose brick/pit/ledge/filled pit/door/chest/lever per `docs/assets/ASSET_PLAN.md`. Started 2026-07-08 by Kayden request with a reproducible first-pass sheet at `game/assets/art/tilesets/tutorial_dungeon_tiles.png`; runtime LDtk wiring remains gated | The dungeon layout was reworked once already; starting source art is OK by request, but replacing runtime tiles should still wait until the room flow is accepted | After T-032 round-2 acceptance | in-progress |
| T-053 | medium | Asset Batch C: overworld character/enemy sprites - hero, healer, quest NPC, forest slime, boss slime, dungeon slime, and slime aggro state per `docs/assets/ASSET_PLAN.md`. First runtime pass now replaces the Hero block and enemy triangles with animated knight/red-ooze SpriteFrames; NPCs, directional movement, distinct slime variants, and aggro art remain. | Needs the accepted Phase 2 loop and should coordinate with T-028's aggro telegraph; not required for Phase 3 save/data logic except exported sprite slots | After T-032 round-2 acceptance; before closing T-028 | in-progress |
| T-054 | medium | Asset Batch D: Phase 3 item/save visuals - forest_key, dungeon_key, shield icons, save crystal sprite, and minimal item/HUD presentation per `docs/assets/ASSET_PLAN.md`. Started 2026-07-08 by Kayden request with a reproducible first-pass sheet at `game/assets/art/icons/item_save_icons.png`; runtime UI/data wiring remains gated on the Phase 3 systems that consume it | These visuals matter once `ItemData`, save crystals, and load flow are real; they should not block the data schema work itself | During Phase 3 after T-034/T-037 start wiring item/save UI | in-progress |
| T-055 | medium | Asset Batch E: Combat MVP visuals - combat units, tactical cursor, move/attack range highlights, target indicator, hit/defend effects, damage popup style, combat menu, HP/turn-order UI per `docs/assets/ASSET_PLAN.md`. Hero knight, Buddy wizard, and red-ooze combat animations are now runtime-wired; cursor/effects/menu polish and distinct enemy variants remain. | Combat assets should be made against the actual Phase 4 interaction model, not the current placeholder/walking-skeleton combat | Start of Phase 4, before judging combat readability | in-progress |
| T-056 | medium | Asset Batch F: party/progression visuals - first recruitable friend's portrait/combat sprite, hero portrait, party menu portraits, starter ability icons, level-up/recruitment accents per `docs/assets/ASSET_PLAN.md` | Party visuals depend on the Phase 5 recruitment and party-menu shape | Start of Phase 5 | planned |
| T-057 | medium | Asset Batch G: Phase 6 first-playable polish - final forest dungeon art, boss presentation, reward presentation, transition polish, and missing interactable states per `docs/assets/ASSET_PLAN.md` | This is polish for the integrated first playable, not a prerequisite for current Phase 3 systems | Phase 6 vertical-slice assembly | planned |
| T-059 | low | Movement-feel polish follow-up: Kayden's 2026-07-08 windowed pass accepted the game but called block movement "a bit chunky" (ambiguous - could be `PushableBlock` push feel or residual grid-step feel). Investigate both: block push has no tween-timing parity with the player's 0.16s step and no push animation; player-side, compare against the T-021/B-07 tuning knobs. Small tuning task, not a rework | Kayden explicitly deprioritized it: "my main complaint right now is combat" - Phase 4 outranks feel polish | After the Phase 4 lane lands, or fold into S-009's dash/roll feel work if that starts first | deferred |
| T-071 | medium | Shops + treasure + currency (D-017): buyable consumables/food, sellable treasure items, and the currency they both need (currency may also join the D-008 defeat penalty per Kayden's earlier note) | No currency system exists yet; shops are real new UI + economy scope, not a data tweak - and D-008 hinted money-loss may want designing alongside it | Phase 5/6 planning, once T-070's drop economy shows what prices/scarcity should mean | planned |
| T-076 | medium | Build the character menu opened by the already-bound `character_menu` action (F/Y). Define the character details, equipment, stats, and party-navigation surface before implementing it. | The 1:1 input anchor is locked now, but the character-menu information architecture belongs with Phase 5 party/progression work. | Phase 5 party/progression UI planning | planned |
| T-079 | medium | Controller-glyph prompt mode. Replace keyboard prompt labels with controller icons when a controller is the active input source; never show slash-separated mixed labels such as `E / A`. Include the five anchor pairs and a keyboard fallback. | Latest playthrough: mixed keyboard/controller text made the required button less clear, not more. Current prompts deliberately show keyboard keys only until this is coherent. | Implemented with T-083 | **done** (2026-07-11) |
| T-058 | medium | Asset Batch H: audio pass - Furnace `.ogg` music and SFX for forest, dungeon, combat, menus, movement, push/jump/fall, chest, doors, save, attack/defend/hit per `docs/assets/ASSET_PLAN.md` | Audio belongs in M6.5 polish unless a specific sound is needed earlier for feel testing | Phase 6 M6.5 polish | planned |

## Bugs

From Kayden's windowed playtests of the slice (2026-07-05, third session).
Fixes implemented on branch `fix/playtest-bugs` (off `integration`),
headless-verified (26/26 smoke test), and **opened as a PR into
`integration`** ([#2](https://github.com/KaydenClark/Dungeon_Friends_Game/pull/2)).
Kayden confirmed B-01/B-02/B-03 in windowed play; B-04 movement is "good
enough" and left `needs-review` for a later feel-tuning pass.

**B-08..B-21 (2026-07-11):** from a front-end-first code QA audit (Claude)
walking the fresh-install player path: boot prompt -> forest -> first NPC ->
first fight -> save -> dungeon. Quick wins were patched the same session on
the working tree (see the 2026-07-11 proof row); every patched row still
needs Kayden's windowed verify and the RUNBOOK headless battery, which could
not be run from the audit environment. Two audit findings were dropped as
already covered by decisions: "advertise Space=jump" (D-022 benches jump) and
"F binding unused" (D-019 reserves it).

| ID | Priority | Bug (reported) | Root cause | Fix | Touches | Status |
|---|---:|---|---|---|---|---|
| B-01 | 1 | NPC dialogue was hard to exit - "no gap or pause"; and (2nd report) still couldn't exit unless walking away as the last line ended | (1) No per-line advance debounce. (2) Deeper cause: the player *polls* interact each frame, so the same press that closed the box re-opened it the next frame (walking away worked because movement out-prioritizes interact) | (1) Per-line `ADVANCE_COOLDOWN_MS` (220ms). (2) `SceneManager.last_ui_close_ms` + a 250ms interact lockout in `player.gd` after any dialogue closes | `game/scripts/ui/dialogue_box.gd`, `game/scripts/autoload/scene_manager.gd`, `game/scripts/overworld/player.gd` | **closed** - Kayden confirmed the fix in windowed play |
| B-02 | 2 | Slime "moved opposite of me" and engaged too soon; and (2nd report) it only moves when the player moves - freezes when you stand still | (1) Pure random-walker (the "opposite" was coincidence). (2) It stepped only on `player_moved` (the synchronized-turn model), so it froze whenever the player did | (1)+(2) Autonomous timer-driven movement (`STEP_INTERVAL` 0.35s): wander until player within `TRACK_RADIUS` (4 tiles), then A*-chase continuously; spawn moved (9,4)->(14,4). **Retires the documented synchronized-turn invariant** (BLUEPRINT updated) | `game/scripts/overworld/overworld_enemy.gd`, `game/scripts/dev/forest_slice.gd` | **closed** - Kayden confirmed the fix in windowed play |
| B-04 | 1 | Walking sometimes moved two cells from a single tap | Player read movement with `is_action_pressed` (held) and re-stepped the instant the 0.15s move tween finished, so any tap held longer than ~0.15s produced a second step | Delayed-auto-shift: a fresh press is always exactly one step; continuous walking only engages after the direction is held past `MOVE_REPEAT_DELAY` (0.2s), then repeats at move cadence. Kayden: "good enough" - **left flagged for a later feel-review pass** (repeat delay/cadence may want tuning). *2026-07-06: that feel pass happened - T-021 reworked the input layer (hold matures through the tween, turn-in-place, last-pressed-wins) with the tap=one-step contract now unit-tested; Kayden's windowed check passed ("night and day")* | `game/scripts/overworld/player.gd` | **closed** (via T-021, 2026-07-06) |
| B-03 | 3 | Combat felt "auto" / hard to track | Only input was a tiny bottom-left Attack/Defend toggle; everything else auto-ran on fixed timers, so it read as a cutscene | Superseded by the T-060..T-064 tactical rebuild: explicit unit movement, range cursor, command menu, abilities/items, and multi-unit turns | `game/scripts/combat/combat.gd` | **needs-review via T-069** - implementation is replaced; Kayden's Phase 4 windowed pass closes feel/readability |
| B-05 | 1 | (T-032 round 1, 2026-07-07) "Random places all over the map I run into and I don't know why... I don't see anything but grass" | `forest.ldtk`'s Ground layer drew the grass tile on all 241 Wall cells - every tree collider was invisible (the tileset's tree tile was never referenced) | Level patch: every Wall cell now draws the tree tile (tree sits on a grass base so edges tile clean); also removed a stray Pit cell sitting under the player spawn | `game/assets/levels/forest.ldtk` | **fixed** (via T-033) - windowed confirm in T-032 round 2 |
| B-06 | 2 | (T-032 round 1, 2026-07-07) "The pressure plate never worked in the game" - blocked Kayden from reaching the pit room | The momentary plate had no readable cause/effect and the player could not hold it while reaching the linked door; headless tests only proved the block-parked path | **Resolved by T-078 screenshot recut:** one flush brass floor plate controls the in-view north gate; player weight opens it, stepping off closes it, and the block holds it open. The room has no pits or second mechanism. | `game/assets/levels/tutorial_dungeon.ldtk`, puzzle scripts/tests | **done** (2026-07-10; windowed owner re-check still rides T-069) |
| B-07 | 1 | (2026-07-08) Movement felt choppy/grid-like again compared with the earlier Godot 4.6 Mac Mini playtest where T-021 felt smooth | Investigation found no movement-code revert after T-021; movement is still Tween-based, but each 64px step only tweened for 0.12s and the first held repeat could wait until the 0.2s tap-safety threshold, leaving a visible beat before continuous walk engaged | Tuned `Player` to move each grid step over `WALK_MOVE_TIME=0.16` and engage held repeat at `MOVE_REPEAT_DELAY=0.17` (about one frame after the first tween instead of ~0.08s); updated `test_player_movement` so bare-player tests use the runtime move time and pin the first-held-repeat hitch budget | `game/scripts/overworld/player.gd`, `game/tests/test_player_movement.gd` | **patched; headless-verified 2026-07-08** (Godot 4.7 on `integration`: unit 18 suites / 109 tests / 371 checks PASS incl. the new repeat-hitch test, smoke 109/109, main boots clean); **closed 2026-07-08** - Kayden's windowed pass accepted the game overall; his residual "block movement is a bit chunky" note (ambiguous between block-push feel and grid-step feel) is parked as T-059, deprioritized behind Phase 4 combat by his call |
| B-08 | 1 | (2026-07-11 audit) Facing is invisible with the runtime idle sprites: `set_facing()` only moves the face marker, which exists only on placeholder bodies, and the sprites have no directional frames - so facing-dependent interact/talk/push is guesswork | `_make_sprite` builds an idle-only `AnimatedSprite2D` with no facing visual; `face_marker` is created only in `_make_body` | **Kayden's call (2026-07-11): overworld actors revert to colored placeholder bodies** (`USE_SPRITE_BODY := false` gates the sprite path) until the Mac Mini asset merge supplies directional-capable art; combat keeps its sprites (no facing there). Long-term fix rides the directional-frames work in T-053/T-055. **Superseded same day:** the Kenney merge re-enabled sprites (`USE_SPRITE_BODY := true`), so a beta facing affordance was added instead - sideways sprite flip in `GridActor.set_facing`, plus a player-only translucent chevron at the faced cell edge that turns gold when the faced cell is interactable (the "E will do something" read). Chevron sits under the sprite, no art touched; directional frames at T-053/T-055 retire it | `game/scripts/overworld/player.gd`, `game/scripts/overworld/grid_actor.gd`, `game/scripts/overworld/overworld_enemy.gd` | **patched 2026-07-11 (rev 2, post-Kenney-merge); needs windowed verify** |
| B-09 | 1 | (2026-07-11 audit) Combat continue prompt said "Press E / Space to continue" but Space was never accepted (CONTINUE handler takes confirm/interact only) - and D-022/D-019 say prompts must not advertise Space anyway | Stale label from before D-022 benched the traversal action | Label is now "Press E to continue"; handler unchanged | `game/scripts/combat/combat.gd` | **patched 2026-07-11; needs windowed verify** |
| B-10 | 1 | (2026-07-11 audit) A companion who died in a previous fight enters the next battle rendered standing and listed like a live unit (HP 0/14), never acts, and reads as a bug | `_build_view` renders every unit; DEAD state/hiding only happened on the in-battle damage path; combat results persist 0 HP back to GameState by design | Units already at 0 HP at battle build are hidden and set DEAD; the party HUD row shows "DOWN". Revive design (post-victory 1 HP? ally-targeted items?) is a separate open question for Phase 5 | `game/scripts/combat/combat.gd` | **patched 2026-07-11; needs windowed verify + revive design decision** |
| B-11 | 1 | (2026-07-11 audit) Entire combat view hardcoded to 1280x720 (arena centering plus fixed menu/status positions) - collides with the locked flexible HD/ultrawide resolution decision; non-16:9 windows misplace or clip the UI | `arena_origin` and panel positions used literal 1280/720 constants | All positions now derive from `get_viewport_rect().size`; offsets chosen to reproduce the old layout exactly at 16:9 | `game/scripts/combat/combat.gd` | **patched 2026-07-11; needs windowed verify at a non-16:9 window size** |
| B-12 | 1 | (2026-07-11 audit) Race: enemies keep acting during room-transition fades, and a chasing enemy in contact range could start an encounter mid-doorway-transition - two coroutines then fight over the fade rect and world container, and the arena can seed from the wrong room | `OverworldEnemy._process` did not check `SceneManager.transitioning`; `start_encounter` did not refuse while transitioning | Both guarded: enemies freeze during transitions, `start_encounter` returns early while `transitioning` | `game/scripts/overworld/overworld_enemy.gd`, `game/scripts/autoload/scene_manager.gd` | **patched 2026-07-11; needs windowed verify (park a slime beside the dungeon doorway and step through)** |
| B-13 | 2 | (2026-07-11 audit) Q on the root combat command menu silently ended the turn: cancel always picked the last entry, which is Wait on the root menu - a reflexive back-out wasted the whole turn | Cancel handler unconditionally emitted the last menu index | Cancel now acts only when the menu's last entry is an actual Back (target/ability/item submenus); on the root menu Q is a no-op and ending the turn takes a deliberate confirm on Wait | `game/scripts/combat/combat.gd` | **patched 2026-07-11; feel-check rides T-069** |
| B-14 | 2 | (2026-07-11 audit) Choosing Continue with an unloadable save silently fell through to a fresh game - reads as save loss with zero messaging | `main.gd` boot flow had no failure branch after `load_game` | On a failed load the game still boots fresh but shows a dialogue explaining the load failed and that the save file was left untouched | `game/scripts/main.gd` | **patched 2026-07-11; needs windowed verify (corrupt `user://saves/slot_1.json` by hand)** |
| B-15 | 2 | (2026-07-11 audit) New Game + first crystal touch silently overwrites slot 1 - the "just trying New Game" player nukes their real save with no confirmation | `save_game` always writes slot 1; no confirm surface exists | Needs a small BootPrompt-style confirm shown only when a save exists and this session did not load it; multi-slot UI stays non-MVP (D-006) | `game/scripts/puzzles/save_crystal.gd`, `game/scripts/autoload/scene_manager.gd` | **ready** |
| B-16 | 2 | (2026-07-11 audit) Companion HP invisible outside combat: the placeholder HUD showed hero HP only, so "is Buddy down?" was unanswerable between fights | HUD line hardcoded to `hero_hp`/`hero_stats` | HUD now renders one HP readout per roster member ("DOWN" at 0 HP), plus the existing XP/items | `game/scripts/main.gd` | **patched 2026-07-11; needs windowed verify** |
| B-17 | 3 | (2026-07-11 audit) No move-undo in combat: confirming a move cell locks it before the action menu opens; genre standard (FE) allows cancel back to the pre-move cell | Turn flow never returns from `_action_menu` to `_pick_cell` | Track the pre-move cell; a Back/cancel on the root menu (once B-13's no-op gets a real meaning) returns the unit and reopens movement. Design belongs with the T-069/Phase 5 combat-UI pass | `game/scripts/combat/combat.gd` | **ready** |
| B-18 | 3 | (2026-07-11 audit) Save-failure dialogue told the player to "check the log" - meaningless to a player | Debug copy in player-facing text | Reworded to in-world copy; diagnostics remain in `push_warning` from `save_game()` | `game/scripts/puzzles/save_crystal.gd` | **patched 2026-07-11** |
| B-19 | 3 | (2026-07-11 audit) Dialogue hint read "E ▸" on every line, including the last - no signal that the next press closes the box | Static hint label | Hint shows "E ▸" mid-dialogue and "E ■" on the final line | `game/scripts/ui/dialogue_box.gd` | **patched 2026-07-11; needs windowed verify** |
| B-20 | 3 | (2026-07-11 audit) `OverworldEnemy.unique_id` docstring claimed defeat is recorded in flags to prevent respawn - directly contradicting D-009 (always respawn) and `defeated()` in the same file | Stale comment from before D-009 | Comment rewritten: uniques always respawn, loot dedup prevents duplicate drops, the id is retained for authoring/future rules. Code unchanged | `game/scripts/overworld/overworld_enemy.gd` | **closed 2026-07-11 (docs-only)** |
| B-21 | 3 | (2026-07-11 audit) Project ships the Mobile feature tag/renderer but has zero touch input - boot prompt, movement, dialogue, and combat are all keyboard/gamepad only; a touch device is a dead-end at the first screen | Input map and all UI are key/button driven; touch was never scoped | Acknowledge as future scope (Phase 6-ish, alongside the T-079 prompt-mode work); this row exists so the gap is tracked rather than forgotten | `game/project.godot`, UI scripts | **deferred** |

## In Progress

| ID | Priority | Task | Owner | Started | Touches | Current note | Proof required | Status |
|---|---:|---|---|---|---|---|---|---|
| - | - | none in progress | - | - | - | - | - | - |

## Blocked

Use this lane for roadblocks, slowdowns, and risks that affect current or
near-term work. If a blocker becomes a stable architectural risk, summarize it
in `BLUEPRINT.md`.

| ID | Task / area | Blocked on | Evidence | Next action | Owner | Status |
|---|---|---|---|---|---|---|
| - | none blocked | - | - | - | - | - |

## Deferred

Valid work that should not be started yet - the stretch goals (from the
retired Gameplan §17; `BLUEPRINT.md`'s build-order step 9 owns the current
sequencing), sequenced. Equipment and elemental/magic are flagged highest-priority-after-
MVP given this project's emphasis on weapon variety and magic (see
`BLUEPRINT.md` -> Design Decisions), but none of these start before the MVP
(Phases 0-6) is complete - see `AGENTS.md` -> Work Selection.

*Pivot note (2026-07-11): three stretch rows were **promoted into the v2
core** and leave this lane - S-002 (elemental interactions) and S-004
(overworld abilities on tagged objects) are absorbed into the shared
material/effect vocabulary (D-031, prototyped at T-093), and S-003
(telegraphed enemy intent) is absorbed into the deterministic combat contract
(D-027, prototyped at T-092). The remaining rows need re-triage against v2
before starting.*

| ID | Task | Deferred until | Why it matters | Revisit trigger |
|---|---|---|---|---|
| S-001 | Equipment system: extend `ItemData` (`equip_slot`, `stat_modifiers`) + an equipment menu | Thesis slice (T-094) proven | Equipment is a named pillar of the v2 progression economy (D-028: "equipment comes from treasure, crafting, shops, and resolved problems") | v2 progression design after T-094 |
| ~~S-002~~ | ~~Elemental counter-system~~ | - | **Promoted to core 2026-07-11 (D-031)** - lives in the shared material/effect vocabulary, prototyped at T-093 | - |
| ~~S-003~~ | ~~Telegraphed combat UI~~ | - | **Promoted to core 2026-07-11 (D-027)** - enemy intent telegraphs are the combat identity, prototyped at T-092 | - |
| ~~S-004~~ | ~~Traversal/Psynergy-style overworld abilities~~ | - | **Promoted to core 2026-07-11 (D-031)** - friends' world verbs on tagged objects ARE the v2 exploration system, prototyped at T-093 | - |
| S-005 | Ikari/Djinn-style resource-gauge mechanics tied to equipment/party collection | MVP (Phases 0-6) complete | Most architecturally involved stretch item - tackle only after S-001 to S-004 are stable | After S-001 to S-004 land |
| S-006 | More dungeons + full overworld map (castle city, mountains, rivers) | MVP (Phases 0-6) complete | Pure content work on the established pipeline; ties directly to the founding vision's world-progression arc | After M6.5 polish pass |
| S-007 | Cosmetic CRT/scanline/color-grading shader (optional, screen-space post-process) | MVP (Phases 0-6) complete | Purely optional polish, never load-bearing; test per-platform with a settings toggle | After M6.5 polish pass |
| S-008 | Roguelike postgame (randomized dungeon generator, stripped-stats mode) | Core game feature-complete | Large, separate system - long-tail replayability only | Once core game is feature-complete |
| S-009 | Dash/roll overworld movement (movement-state roadmap row 4, Kayden 2026-07-06) | Phase 2 tutorial dungeon (T-027) shipped and T-021 movement feel confirmed in windowed play | "Makes overworld feel better" - but it's polish on top of the core walk feel, which must be right first | After T-027 lands and Kayden signs off on T-021 feel |
| S-010 | Swim / water traversal (movement-state roadmap row 5, Kayden 2026-07-06) | Post-MVP world expansion | "First major traversal upgrade" - pairs naturally with the rivers region of the world-progression arc (forest -> castle city -> mountains -> rivers) | When S-006 world expansion reaches the rivers region |

## Done

| ID | Task | Completed | Result | Proof row |
|---|---|---|---|---|
| T-001 | M0.2: Godot project scaffold - Mobile renderer, Nearest filter, 240x160 viewport/integer scale, `main.tscn` with `SceneManager` autoload + placeholder background | 2026-07-05 | pass | See Proof Log row 2026-07-05 |
| T-005 | Repo doc update: replace fixed 240x160/GBA-locked resolution language with flexible HD/ultrawide display language | 2026-07-05 | pass (docs); resolution switch confirmed by a real headless run in the second session | See Proof Log rows 2026-07-05 (T-005/T-006/T-007) and (T-005/T-006/T-007 follow-up) |
| T-006 | Project settings: `game/project.godot` updated to `canvas_items`/`expand`/`fractional` at a 1280x720 design reference (Nearest filter unchanged) | 2026-07-05 | pass - confirmed by a real headless run in the second session (`--import` + `main.tscn --quit-after 1` both exit 0) | See Proof Log rows 2026-07-05 (T-005/T-006/T-007) and (T-005/T-006/T-007 follow-up) |
| T-007 | Display test scene: `game/scenes/dev/display_scaling_spike.tscn` + `game/scripts/dev/display_scaling_spike.gd` - placeholder tile grid + resolution label, sized dynamically to `get_viewport_rect().size` | 2026-07-05 | pass - confirmed by a real headless run at all 3 resolutions in the second session; note headless mode can't actually validate per-resolution viewport sizing (see RUNBOOK caveat) | See Proof Log rows 2026-07-05 (T-005/T-006/T-007) and (T-005/T-006/T-007 follow-up) |
| T-015 | Design clarification: lock in GBA-fantasy-adventure visual language (8x8/16x16 tile logic), confirm the general explore -> interact -> act -> consequence loop, and define grid-based/per-unit-initiative/d10 combat | 2026-07-05 | pass (docs only, no code touched) | See Proof Log row 2026-07-05 (T-015) |
| T-009 | Input map: 8 actions (`move_up/down/left/right`, `interact`, `confirm`, `cancel`, `menu`) with keyboard bindings in `game/project.godot` | 2026-07-05 | pass - smoke test asserts all 8 exist with >=1 binding | See Proof Log row 2026-07-05 (T-009) |
| T-010 | Grid-snapped player movement, wall collision, camera follow (`GridActor`/`Player` + `RoomGrid`, Tween-based, AStarGrid2D no-diagonal pathfinding) | 2026-07-05 | pass - headless smoke-verified | See Proof Log row 2026-07-05 (T-010) |
| T-012 | Interaction system: faced-cell `interact` (NPC dialogue via `DialogueBox`, locked door), bump-contact enemy encounter trigger. Cell-occupancy checks used instead of `Area2D` (fits the everything-rests-on-grid invariant); revisit if free-positioned triggers are ever needed | 2026-07-05 | pass - headless smoke-verified | See Proof Log row 2026-07-05 (T-012) |
| T-013 | Combat scene + transition - deliberately built past the "empty shell" scope on Kayden's play-testable instruction: minimal but real grid battle (initiative by speed, AStarGrid2D movement, melee-adjacent, d10 rolls, Attack/Defend) with fade transition and exact-position overworld restore | 2026-07-05 | pass - original walking skeleton; placeholder formulas and 1v1/menu flow superseded by T-060..T-064 | See Proof Log row 2026-07-05 (T-013) |
| T-014 | Proof log rows for the completed task batch | 2026-07-05 | pass - rows appended below | This row |
| T-016 | First-playable slice integration (forest room + NPC + enemy + combat + key + door + goal) with end-to-end headless smoke test | 2026-07-05 | pass - 26/26 checks, exit 0; Kayden's windowed play-check also passed | See Proof Log row 2026-07-05 (T-016) |
| T-017 | Promote `integration` -> `main`: committed the slice to a new `integration` branch, Kayden play-checked it, then fast-forward-merged into `main` on his explicit go-ahead | 2026-07-05 | pass - `main` at `097ced0`, all 3 headless checks re-confirmed post-merge | See Proof Log row 2026-07-05 (T-017) |
| T-018 | Expanded ~5-minute playtest (Kayden's request, fourth session): 34x20 tree-filled forest, 7 roaming slimes + leashed Boss Slime holding the key by the door, red-triangle enemy art (overworld + combat), healer NPC, HP/XP/key HUD, boss/regular loot split (`boss_slime.tres` / `forest_slime.tres`) | 2026-07-05 | pass (headless) - smoke test rebuilt to 34 checks, 5/5 consecutive PASS runs; later windowed slice acceptance recorded under T-032 | See Proof Log row 2026-07-05 (T-018) |
| T-003 | M1.1 (core): grid unit decided - **16x16 art pixels @ 4x = the 64px runtime cell**; first real art generated deterministically via `assets/art/_scripts/generate_test_tileset.gd` (grass/tree/path/cave-floor/cave-wall tiles + oversized-head hero sprite, GBA-bright per Visual Language); Aseprite Lua batch exporter (`export_sheets.lua` + `.sh` wrapper) written and ready. **Aseprite itself not installed** - the .aseprite-source half of the pipeline waits on Kayden's install decision (paid app) | 2026-07-06 | pass - PNGs import clean (exit 0, 0 errors); art eyeballed at 8x and reads correctly | See Proof Log row 2026-07-06 (T-003) |
| T-004 | M1.2: `heygleeson/godot-ldtk-importer` v2.0 installed to `game/addons/` + enabled; minimal one-room LDtk project (`assets/levels/test_room.ldtk`, 20x12 @16px, Wall IntGrid border + Ground tiles from the T-003 tileset) authored as bootstrap JSON (LDtk app not installed - free, install when authoring starts) | 2026-07-06 | pass - headless import exit 0 with 0 warnings on 2nd pass; instantiated scene verified: `LDTKWorld > TestRoom > Wall-values (TileMapLayer, 60 cells = exact border)` + `Ground (TileMapLayer, 240 cells)`, wall corners set / interior empty | See Proof Log row 2026-07-06 (T-004) |
| T-008 | Folder hygiene audit: `game/` root holds exactly the 8 expected dirs (`addons/ assets/ data/ scenes/ scripts/ shaders/ tests/` + `project.godot`), `.godot/` ignored, no strays; only drift was `tests/` missing from the BLUEPRINT Directory Map (added) | 2026-07-06 | pass | Audit output in the 2026-07-06 session; `BLUEPRINT.md` Directory Map updated |
| T-022 | Boss-door room transition: `SceneManager.boot_room/enter_room/exit_room` room stack (suspend-not-free, mirrors the combat pattern), forest doorway -> LDtk-built cave stub room (`DungeonStubRoom` + `cave_room.ldtk`: Wall IntGrid -> RoomGrid blocking, 4x art scale - the first LDtk-pipeline room in the live game, pre-proving T-011's wiring), doorway gap exits back to the forest with state fully preserved (position, dead boss, open door) | 2026-07-06 | pass - smoke test extended to 47 checks covering the full round trip, 5/5 consecutive PASS; windowed transition-feel check is Kayden's | See Proof Log row 2026-07-06 (T-022) |
| T-021 | Movement feel rework to the Zelda/Pokemon bar: pure `_movement_intent` state machine (hold matures through the tween - no dead stop; turn-in-place; last-pressed-wins direction stack; buffered same-direction presses; tap=one-step preserved), unit-tested via `test_player_movement` (12 tests, 27 checks). Subsumed and closed B-04 | 2026-07-06 | pass - headless 3/3 unit + 5/5 smoke; **Kayden's windowed feel-check passed: "night and day different on the movement"** | See Proof Log rows 2026-07-06 (T-021) and (T-021 windowed) |
| T-019 | First-party headless unit-test layer under `game/tests/` (base `gd_test.gd`, reflective runner `run_tests.tscn`, 9 suites) covering pure/deterministic logic (combat d10 math, `RoomGrid`, `GridActor.try_step`, `.tres` data + boss-key/door invariant, dialogue advance/cooldown, enemy AI branches, SceneManager victory rewards/heal); test-the-real-path refactors extracted `CombatScene.hit_threshold`/`needed_roll`/`attack_damage` and `SceneManager.apply_victory_rewards`/`heal_hero_to_full` | 2026-07-05 | pass (headless) - `UNIT TESTS: PASS` (9 suites, 54 tests, 149 checks, 0 failed); negative control gates (exit 1); slice smoke test still 34/34. On branch `test/automated-test-suite` | See Proof Log rows 2026-07-05 (T-019) and (T-019 cont.) |
| T-023 | Phase 2 M2.1a: `PushableBlock` - one-cell bump-push (player never steps on the push frame), refused into walls/occupants/edges/doorway-protected cells, sinks into pits and fills them permanently, lever/dev reset-to-start | 2026-07-06 | pass (headless) - unit suite `test_pushable_block` + smoke steps 10-11 | See Proof Log row 2026-07-06 (Phase 2 primitives) |
| T-024 | Phase 2 M2.1b: `PressurePlate` (momentary, pressed by any occupant) + `PuzzleController` (plate->door wiring at ready) + `Lever` (puzzle-reset escape valve); `LockedDoor` gained link_id/required_key/plate-driven held-open with deferred re-lock (never closes onto an occupant) | 2026-07-06 | pass (headless) - unit suites `test_pressure_plate` + `test_tutorial_softlock` (exhaustive wedge check); smoke step 10 | See Proof Log rows 2026-07-06 (Phase 2 primitives) and (T-027) |
| T-025 | Phase 2: `jump` input action (Space only after the locked 2026-07-07 keyboard revision; supersedes initial Alt/C - D-003), pit cells (block walking/pathing, non-lethal), 1-cell Tween-arc hop over a jumpable gap with in-place hop on refusal, block-fills-pit | 2026-07-06 | pass (headless) - unit suite `test_jump`; smoke step 11 (2-wide unjumpable, fill + jump crossing); windowed feel-check is Kayden's (T-032) | See Proof Log row 2026-07-06 (Phase 2 primitives) and 2026-07-10 input restoration |
| T-026 | Phase 2: `Chest` interactable - key-locked (LockedDoor pattern), visible from the start, opens once, reward de-duplicated, opened state persists via flags; `dungeon_slime.tres` carries the chest key | 2026-07-06 | pass (headless) - unit suite `test_chest`; smoke step 13 | See Proof Log row 2026-07-06 (Phase 2 primitives) |
| T-029 | Phase 2 (D-004): party defeat restarts from the beginning - `SceneManager.handle_defeat` -> `reset_session_state` (XP/inventory/flags/HP) + `restart_game` (frees the room stack, reboots via `boot_factory`) | 2026-07-06 | pass (headless) - unit test (state wipe) + smoke step 14 (forced defeat -> fresh forest at spawn, boss respawned) | See Proof Log row 2026-07-06 (Phase 2 primitives) |
| T-031 | LDtk entity post-import pipeline (D-002): `scripts/ldtk/entities_post_import.gd` instantiates the matching game object per entity (conventions in-script), `entities_post_import` wired in each .ldtk's import params, `LdtkRoom` base adopts spawned nodes into the runtime grid (IntGrid Wall/Pit -> blocking/pits, refs, PuzzleController, doorway markers + no-block cells, flag-based persistence) | 2026-07-06 | pass (headless) - unit suite `test_ldtk_pipeline` against the committed `entity_test_room.ldtk` fixture (one of every entity type adopted at the right cells with fields carried) | See Proof Log row 2026-07-06 (T-031/T-011) |
| T-011 | Forest rebuilt through the LDtk pipeline: `forest.ldtk` (same 34x20 layout; tiles + Wall IntGrid + all entities as LDtk instances), `ForestRoom extends LdtkRoom` replaces the code-built `forest_slice.gd` (deleted, with the T-022 `dungeon_stub_room.gd`/`cave_room.ldtk` the tutorial supersedes) | 2026-07-06 | pass (headless) - full smoke test runs against the LDtk forest (walk/collide/NPC/fights/key/door checks unchanged in spirit, now 94/94 overall) | See Proof Log row 2026-07-06 (T-031/T-011) |
| T-030 | Dev tools: `DebugOverlay` (debug builds only via `OS.is_debug_build()`, hidden until F1) - warps (Forest/Hub/Pit/Fight), reset room puzzle, grant forest_key/chest_key/shield, heal, skip-combat toggle (`SceneManager.skip_combat` -> instant victory) | 2026-07-06 | pass (headless) - unit suite `test_debug_overlay` (hidden by default, grant dedup, reset delegation); windowed F1 pass is Kayden's (T-032) | See Proof Log row 2026-07-06 (T-030) |
| T-050 | Godot 4.6.x -> 4.7.x engine upgrade (Kayden's explicit decision, supersedes the locked 4.6.x): `project.godot` features bumped to 4.7, LDtk importer 2.0 re-verified, locked-decision docs updated, full suite re-run clean on 4.7 | 2026-07-07 | pass (headless) | See Proof Log row 2026-07-07 (T-050) |
| T-036 | Phase 3 M3.1/§3.1: `GameState` Resource holds the whole mutable session (party_roster/levels/xp/hp, inventory, flags); SceneManager forwards `hero_hp`/`total_xp`/`inventory`/`flags` to it and `add_item()` is the one deduped inventory write; reset swaps a fresh GameState | 2026-07-07 | pass (headless, behavior-neutral) | See Proof Log row 2026-07-07 (T-036) |
| T-033 | Playtest-feedback rework (T-032 round 1, Kayden 2026-07-07): forest tree tiles on every Wall cell (B-05) + spawn-pit removal + open-field tree clusters; pressure plate ON HOLD (B-06); dungeon rescoped to 4 rooms - hub 13-brick wall with one movable brick (`PushableBlock.Movable` field, new fixed-brick primitive), new ChestRoom behind a north dungeon_key door, pit room reworked to 2 jumpable ledges + 2-wide chasm, guardian drops dungeon_key; solver made jump- and fixed-brick-aware; new `screenshot_tour.tscn` dev scene for per-room PNG demo artifacts | 2026-07-07 | pass (headless + screenshot review); Kayden's T-032 round-2 windowed re-check accepted it 2026-07-08 | See Proof Log row 2026-07-07 (T-033) |
| T-034 | M3.1: `ItemData` + `ItemLibrary` + `{id: qty}` inventory (first Phase 4-lane task) | 2026-07-08 | pass - red/green; unit 19 suites / 115 tests / 391 checks, smoke 109/109, main boots clean (Godot 4.7) | See Proof Log row 2026-07-08 (T-034) |
| T-035/T-043/T-044/T-045/T-046 | Phase 4 data foundation: AbilityData, stat/range alignment, EncounterData/MapMeta stubs, XP curve, shield-gated Defend | 2026-07-08 | pass - Resource instances load; progression and command gates covered by the 22-suite battery | See Proof Log row 2026-07-08 (Phase 4 build sprint) |
| T-060/T-061/T-062/T-063/T-064 | Phase 4 tactical-combat core: real d10 CombatMath, TurnManager/two-layer FSM, local-terrain multi-unit arena with Buddy, range cursor/highlights, Attack/Ability/Item/Defend | 2026-07-08 | pass (headless core) - unit 22 suites / 137 tests / 483 checks; slice smoke 109/109 on 5/5 runs | See Proof Log row 2026-07-08 (Phase 4 build sprint) |
| T-020 | M1.4: HD/ultrawide stretch re-validation with the real M1.1 tileset at 1280x720 / 1920x1080 / 3440x1440 | 2026-07-08 | pass - Kayden's windowed run confirmed the grid reads fine at all three; `expand` kept, no `keep`+letterbox fallback. Phase 1 fully done | See Proof Log row 2026-07-08 (T-020/T-032) |
| T-032 | Phase 2 windowed acceptance (Kayden), rounds 1+2 | 2026-07-08 | round 1 (2026-07-07) drove the B-05/B-06 findings + 4-room rescope (built as T-033); round 2 (2026-07-08) passed - **Phase 2 accepted**. Feel note "block movement is a bit chunky" -> T-059; combat complaint -> Phase 4 planning | See Proof Log rows 2026-07-07 (T-032 round 1) and 2026-07-08 (T-020/T-032) |
| T-027 | Original Phase 2 tutorial-dungeon integration: 3-room plate/chest/pit/fight layout with state-preserving forest return | 2026-07-06 | pass (headless) - original 94/94 proof; superseded by T-033's accepted 4-room brick-wall/chest-room layout, with final windowed acceptance under T-032 | See Proof Log row 2026-07-06 (T-027) |

## Documentation Check

Documentation is part of done. Before marking a task complete, check:

| If the task changed... | Update or confirm |
|---|---|
| Vision, architecture, data model, invariants, design decisions | `BLUEPRINT.md` |
| Task queue, blockers, deferred work, proof of completed work | `TASKBOARD.md` |
| Setup, run, build/export, verification procedure | `RUNBOOK.md` |
| Public-facing usage, project description | `README.md` |
| Agent rules, scope, authority, verification policy | `AGENTS.md` |
| Asset inventory, production phase, generated image prompt provenance | `docs/assets/ASSET_PLAN.md` and `docs/assets/IMAGE_PROMPTS.md` |

If no docs need edits, record `Docs checked; no update needed` in the final
response and in the proof row's `Docs` field.

## Proof Log

Append a row when a task changes durable project state or produces durable
verification evidence. Use actual results, not stale claims. Milestone tasks
must fill the Demo column with a <1-minute demo artifact; non-milestone rows
may use `n/a`.

**Archival policy.** The proof log is append-only, but it should not grow
without bound. When it passes ~30 rows, move the oldest rows (keep the most
recent ~30 here) into `TASKBOARD_ARCHIVE.md`, preserving them verbatim under a
dated heading.

*Older rows (through the 15 oldest as of 2026-07-09) live verbatim in
`TASKBOARD_ARCHIVE.md`.*

| Date | Task ID | Agent | Proof | Demo | Result | Docs | Remaining gap |
|---|---|---|---|---|---|---|---|
| 2026-07-05 | T-017 | Claude | Kayden completed his windowed play-check of the first-playable slice and confirmed it's ready. Committed the slice to a new `integration` branch (commit `097ced0`), then fast-forward-merged `integration` -> `main` (`git merge --ff-only integration`, 199561c..097ced0, no conflicts) on Kayden's explicit "promote integration" instruction. Re-ran all 3 headless checks (`--import`, `main.tscn --quit-after 3`, `slice_smoke_test.tscn`) on `main` post-merge - all exit 0, smoke test still 26/26 | Local `main` now at 097ced0, matches what Kayden play-tested on `integration` | pass | `AGENTS.md`/`RUNBOOK.md`/`BLUEPRINT.md` branch-flow rows updated to the `integration`-staging convention prior to this merge; this row | `main` is 5 commits ahead of `origin/main` - not yet pushed, pending Kayden's confirmation |
| 2026-07-06 | T-021 | Claude | Movement feel rework on `feature/movement-feel` (stacked on `test/automated-test-suite`, whose unit harness it needs). Rewrote `player.gd`'s input layer around a pure `_movement_intent(dir, delta)` state machine the real `_process` calls: (1) hold time now matures *through* the move tween, killing the 0.2s dead stop after the first held step (residual tap-safety beat: ~0.08s, tunable); (2) Pokemon-style turn-in-place - tapping a new direction faces it without stepping, holding past `TURN_DELAY` (0.1s) walks, and the turn time counts toward the walk so there's no second pause; (3) last-pressed-wins direction stack (rolling between keys mid-walk is seamless, releasing falls back to the still-held key); (4) fresh same-direction presses during a tween are buffered to land the frame the move ends; (5) B-04's tap=exactly-one-step contract preserved and now unit-tested. New suite `test_player_movement` (12 tests, 27 checks) drives the real intent function with hand-fed deltas + `Input.action_press` rollover. Verified (Godot 4.6.3): unit tests `PASS` **3/3 consecutive runs** (10 suites, 65 tests, 176 checks, exit 0); negative control (corrupt the tap=one-step expectation) -> `CHECK FAILED`/suite FAIL, then restored; `--import` exit 0, 0 error lines; `main.tscn --quit-after 3` exit 0; `slice_smoke_test.tscn` `PASS (34/34)` **5/5 consecutive runs** | `cd game && /Applications/Godot.app/Contents/MacOS/Godot --headless --path . tests/run_tests.tscn` (~2s) then walk around in `main.tscn`: tap to turn, hold to walk, roll between directions | pass (headless); **windowed feel-check is Kayden's gate** - tuning knobs are `MOVE_REPEAT_DELAY` (0.2) and `TURN_DELAY` (0.1) in `player.gd` | `RUNBOOK.md` unit-test counts + suite list; `TASKBOARD.md` this row + T-021 gated + B-04 -> gated-via-T-021; no `BLUEPRINT.md` change (implements the already-documented feel bar) | Windowed judgment: does turn-in-place feel right at 0.1s, is the ~0.08s first-step beat perceptible, does hold-walk cadence read smooth? Camera smoothing untouched; deliberate swallow: a tap *released* mid-tween is dropped (anti-double-step) - flag if quick double-taps feel eaten |
| 2026-07-06 | T-021 (windowed) | Claude | Kayden ran the windowed feel-check and confirmed: "This feels night and day different on the movement!" - T-021 moved to Done, B-04 closed, no tuning of `MOVE_REPEAT_DELAY`/`TURN_DELAY` requested | Kayden's own play session | pass | `TASKBOARD.md` this row + Done lane + Bugs lane | none - T-021 complete |
| 2026-07-06 | T-022 | Claude | Boss-door room transition on `feature/movement-feel`. `SceneManager` gained a room stack (`boot_room`/`enter_room`/`exit_room`, `transitioning` flag): entering hides+disables the current room (never frees - same context-preservation pattern as combat), exiting frees the sub-room, restores the previous one, and re-currents its player camera. Forest doorway cell now triggers entry into `DungeonStubRoom` - the **first LDtk-pipeline room in the live game**: instantiates `cave_room.ldtk` (bootstrap-generated cave variant, 59-wall border with a south doorway gap), scales art 4x, feeds the Wall IntGrid into `RoomGrid.set_blocked` - exactly the wiring T-011 will apply to the forest. Player input is gated during transitions. Verified (Godot 4.6.3, absolute `--path`): `--import` exit 0; unit tests PASS 3/3 (10 suites, 65 tests, 176 checks - untouched by this change); `main.tscn` boot clean; smoke test extended from 34 to **47 checks** (door -> doorway step -> stub room entered + flag + entry cell + camera switch + forest hidden-but-preserved + 59 blocked cells + wall unwalkable -> step out -> forest restored + exact doorway cell + camera back + boss still dead + door still open) - **PASS 47/47 on 5/5 consecutive runs**. Debug note: an earlier "hang" was the shell running Godot with `--path .` from the repo root (no project.godot) - not a code bug; RUNBOOK now documents the gotcha | `cd game && Godot --headless --path . scenes/dev/slice_smoke_test.tscn` -> 47/47; or windowed: beat the boss, open the door, walk through | pass (headless); windowed transition-feel check is Kayden's (fade pacing; camera-zoom polish is the M2.3-era refinement) | `BLUEPRINT.md` scene-contract row (dungeon stub room); `RUNBOOK.md` smoke expectation 47/47 + cwd gotcha; `TASKBOARD.md` this row + Done lane | Camera-pan/zoom transition polish deferred to Phase 2 M2.3 proper; stub room content is placeholder until T-027; `goal_cells`/X tiles are now decorative cave-mouth flooring |
| 2026-07-06 | T-020 (prep) | Claude | Display spike upgraded for M1.4: draws the real M1.1 tiles (all 5 via region sprites at 4x, nearest-filtered) + the hero sprite instead of ColorRects, with a fallback if textures are missing. Headless run prints `real_art=true` (viewport sizing still not headless-checkable - known caveat from the T-005/6/7 follow-up row). **Windowed 3-resolution judgment remains Kayden's** - T-020 set to `gated` | Windowed: `Godot --path game scenes/dev/display_scaling_spike.tscn --resolution 3440x1440` (and 1280x720 / 1920x1080) | prep pass; final verdict pending Kayden | `TASKBOARD.md` T-020 row + this row | Kayden's windowed run at 720p/1080p/1440p-ultrawide; decide `expand` vs `keep`+letterbox if 21:9 over-reveals |
| 2026-07-06 | T-003 | Claude | M1.1 with a documented deviation: **Aseprite is not installed on this Mac** (checked `/Applications` + PATH; paid app, purchase is Kayden's call per AGENTS no-paid-services). Delivered the milestone's substance without it: (1) grid unit decided - 16x16 art px @ 4x = 64px runtime cell (`RoomGrid.TILE`), BLUEPRINT Visual Language/Architecture rows updated; (2) real test art generated deterministically by `assets/art/_scripts/generate_test_tileset.gd` (Godot headless `Image` drawing, fixed seeds): `tilesets/test_tiles.png` (80x16: grass, tree-on-grass, path, cave floor, cave wall) + `sprites/test_hero.png` (16x16, outlined oversized-head hero); (3) Aseprite batch pipeline pre-built (`export_sheets.lua` + `.sh` wrapper that fails with install instructions) targeting the same output paths, so installing Aseprite later swaps the source of truth without touching anything downstream. Verified: generator exits 0; `--import` exit 0, 0 error lines, both textures imported; art visually checked at 8x nearest-neighbor (grass/tree/path/cave read as GBA-bright, toy-like; hero silhouette+outline correct) | 8x preview renders + `Godot --headless --path . --script assets/art/_scripts/generate_test_tileset.gd` (~1s) | pass (art + import); **gap: .aseprite sources + real Aseprite export await the install decision** | `BLUEPRINT.md` Art + Visual Language rows (grid unit decided, install status); `TASKBOARD.md` this row + Done lane | Kayden: install Aseprite (~$20, or Pixelorama free) when ready; art quality itself is placeholder-grade by design - real art passes come later on this pipeline |
| 2026-07-06 | T-004 | Claude | M1.2 complete: `heygleeson/godot-ldtk-importer` v2.0 (upstream `0ecab8d`, Godot 4.1+/LDtk 1.5.3, emits `TileMapLayer` - locked-decision compliant) copied into `game/addons/ldtk-importer/` and enabled in `project.godot`. LDtk desktop app is not installed (free) - authored `assets/levels/test_room.ldtk` as bootstrap LDtk-1.5.3 JSON instead (scratchpad generator): 20x12 room @16px, Wall IntGrid border (60 cells), Ground Tiles layer (240 tiles from `test_tiles.png`). Verified: 1st `--import` exit 0 (benign first-pass UID warning), 2nd `--import` exit 0 with **0 warnings/errors**; instantiation script confirms `LDTKWorld > TestRoom > Wall-values (TileMapLayer, 60 used cells)` + `Ground (TileMapLayer, 240 used cells)`, wall corners set, interior empty. Note: "collision" in this project is RoomGrid cell-blocking, not physics - the Wall IntGrid is readable per-cell, which is what T-011 consumes. Regression battery after the addon: unit tests PASS, `main.tscn` boots, smoke 34/34 | `Godot --headless --path . --import` then the verify script (tree + cell counts printed) | pass | `BLUEPRINT.md` Levels row (installed + verified, LDtk-app note); `TASKBOARD.md` this row + Done lane | LDtk desktop app install (free) before real map authoring; T-011 wires `Wall-values` into `RoomGrid` |
| 2026-07-06 | T-023/T-024/T-025/T-026/T-029 (Phase 2 primitives) | Claude | Puzzle primitives + defeat restart on `feature/phase2-tutorial-dungeon` (branched off `integration`, includes the round-3 decision docs). `RoomGrid` gained pits (block walking/pathing, fillable), an occupancy-change signal (plates listen), occupy/vacate, no-block cells, teleport, and reset_puzzle. New: `PushableBlock` (bump-push one cell; sinks into pits and fills them permanently), `PressurePlate` (momentary; pressed by player *or* block), `PuzzleController` (plate->door wiring; adopts pre-pressed plates), `Lever` (reset escape valve), `Chest` (key-gated, one-time reward, flag persistence); `LockedDoor` reworked (per-door key, link ids, plate-driven held-open with deferred re-lock so a door never closes onto an occupant, custom locked lines, open-state persistence); `Player` gained the `jump` button (Alt/C - D-003; exactly 1 cell, Tween arc, in-place hop on refusal) and push-on-bump; `SceneManager` gained `handle_defeat`/`restart_game`/`reset_session_state` (D-004: defeat = restart from the beginning via main.gd's boot_factory) and the `skip_combat` dev hook. Verified (Godot 4.6.3): 4 new unit suites (`test_pushable_block`, `test_pressure_plate`, `test_jump`, `test_chest`) + `test_scene_manager` reset test - all PASS; negative control (corrupted push expectation) -> `UNIT TESTS: FAIL` then restored | `cd game && Godot --headless --path . tests/run_tests.tscn` | pass (headless); windowed jump/push feel is Kayden's (T-032) | `BLUEPRINT.md` Known Risks soft-lock row (mitigations built); `RUNBOOK.md` unit-test list; `TASKBOARD.md` this row + Done lane | Exact d10 combat formula still Phase 3/4; pull (vs push) deliberately out of scope |
| 2026-07-06 | T-031/T-011 | Claude | LDtk entity pipeline + LDtk forest. `scripts/ldtk/entities_post_import.gd` (importer's entity-template pattern, set as `entities_post_import` in each .ldtk's import params) instantiates the matching game object per LDtk entity and stamps cell/fields metadata; entity-layer conventions documented in-script (PlayerSpawn/Npc/Enemy/LockedDoor/PushableBlock/PressurePlate/Chest/Lever/Doorway + fields). Runtime `LdtkRoom extends RoomGrid` instantiates the imported world at 4x, feeds Wall/Pit IntGrids into the grid, adopts spawned entities (reparent + register + wire refs/PuzzleController), marks doorway cells + approach cells block-free, spawns the player, and restores persisted state (opened doors/chests, slain unique enemies) from `SceneManager.flags`. Game fields made `@export` so import-time values survive scene packing. `forest.ldtk` (bootstrap JSON via committed `assets/levels/_scripts/generate_levels.py` - exact old 34x20 layout, 13 entities) + `ForestRoom` replace `forest_slice.gd`; T-022's `dungeon_stub_room.gd`/`cave_room.ldtk` retired (superseded by T-027's real rooms, same wiring). Verified: `--import` clean with `Entity Post-Import` running on all 5 entity layers; unit suite `test_ldtk_pipeline` (37 checks) proves the committed `entity_test_room.ldtk` fixture adopts one of every entity type at the right cells with fields carried; full smoke passes against the LDtk forest | Unit run + smoke battery (see T-027 row) | pass | `BLUEPRINT.md` Levels/scene-contract rows; `TASKBOARD.md` this row + Done lane | .ldtk files remain generator-authored until Kayden hand-authors in the LDtk app; multi-file worlds consolidate into `world.ldtk` at that point |
| 2026-07-06 | T-030 | Claude | Dev overlay `scripts/dev/debug_overlay.gd`: instantiated by main.gd only when `OS.is_debug_build()` (never in release exports), hidden until F1. Actions: 1-4 warp (fresh Forest / Hub / Pit / Fight - tears down the room graph and boots the target; dungeon exits no-op on the empty stack, documented dev-only), 5 reset room puzzle, 6-8 grant forest_key/chest_key/shield (deduplicated), 9 heal, 0 skip-combat toggle (`SceneManager.skip_combat` -> instant victory + loot, no combat scene). HUD now lists the full inventory. Verified: unit suite `test_debug_overlay` (hidden by default, grant dedup, reset delegates to the real reset_puzzle); skip_combat default-off pinned in `test_scene_manager` | Windowed: run `main.tscn`, press F1 | pass (headless); windowed key-pass is Kayden's (T-032) | `RUNBOOK.md` dev-tools note; `TASKBOARD.md` this row + Done lane | Warp list is hardcoded to the 4 current rooms - extend as rooms are added |
| 2026-07-07 | T-050 | Claude + Kayden | Godot engine upgrade 4.6.x -> 4.7.x, Kayden's explicit decision superseding the locked 4.6.x (a Locked Technical Decision change, flagged and made deliberately - not drift). Trigger: the local `/Applications/Godot.app` was upgraded to `4.7.stable.official.5b4e0cb0f` mid-session, which auto-bumped `project.godot`; rather than fight it, Kayden chose to move the project to 4.7. Changes: `project.godot` `config/features` -> `4.7` (clean single-line bump, no editor-reorder churn); locked-decision docs updated to 4.7.x (`AGENTS.md` quick-reference, `BLUEPRINT.md` Engine row + LDtk-risk row + Design Decisions row, `RUNBOOK.md` required-tools + troubleshooting + Windows-export line, `README.md`, `docs/planning/Gameplan.md` revision note + target line). Historical records left as-is (proof-log rows verified-on-4.6.3, `docs/LEGACY_HARNESS.md`, the dated research audit - all accurate for when written). Addon untouched by hand (locked scope) - LDtk importer 2.0 just re-verified. Verified (Godot 4.7): wiped `.godot/imported` + full `--import` exit 0, zero errors/warnings/deprecations, importer 2.0 reimported all 4 `.ldtk` worlds; unit `PASS 18 suites/108 tests/369 checks` (lone warning is the intentional bad-wiring case in `test_pressure_plate`); slice smoke `PASS 109/109` on 3/3 runs; `main.tscn` boots clean | `cd game && Godot --version` (-> 4.7) then `Godot --headless --path . --import && ... tests/run_tests.tscn && ... scenes/dev/slice_smoke_test.tscn` | pass (headless) | `AGENTS.md`, `BLUEPRINT.md`, `RUNBOOK.md`, `README.md`, `docs/planning/Gameplan.md`, `TASKBOARD.md` (this row + T-050 Done) | Windowed editor open on 4.7 is Kayden's to eyeball (headless can't catch editor-UI regressions); no code changes were needed for 4.7 compatibility |
| 2026-07-07 | T-036 | Claude | GameState Resource refactor (Phase 3 M3.1/§3.1), the foundation the rest of Phase 3 hangs off. New `game/scripts/data/game_state.gd` (party_roster/party_levels/party_xp/party_hp dicts + inventory + flags, party-shaped from day one so SaveData won't schema-churn at Phase 5). `SceneManager.state: GameState` is the single source of truth; `hero_hp`/`total_xp`/`inventory`/`flags` became forwarding properties over it, `add_item()` is the one deduped inventory write path (chest.gd + debug_overlay.gd migrated off `.append`), `reset_session_state()` swaps a fresh GameState. Red/green: new `test_game_state` suite (per-instance container isolation, property forwarding, add_item dedup, reset swap) written first. Verified (Godot 4.6.3): `--import` exit 0 (an earlier `import:1` was two colliding background import processes, not a code error - re-ran clean in the foreground); unit `PASS 18 suites/108 tests/369 checks`; smoke `PASS 109/109` - behavior-neutral, exactly the intent | `cd game && Godot --headless --path . tests/run_tests.tscn` then `... scenes/dev/slice_smoke_test.tscn` | pass (headless, behavior-neutral) | `BLUEPRINT.md` Architecture single-autoload note (GameState detail); `TASKBOARD.md` T-036 -> Done + this row | Inventory is still a PackedStringArray - T-034 upgrades it to `{id: qty}` + ItemData next |
| 2026-07-07 | Phase 3 decisions locked | Kayden + Claude | Kayden resolved D-006..D-011 (see Pending Decisions for resolutions + his rationale verbatim): JSON saves (delegated pick), shield-unlocks-Defend, checkpoints/XP-loss-never-below-level/pit-falls, enemies-respawn-on-room-reset, stubs-now, minimal boot prompt + go-anywhere dev warps. Board updated: T-037/T-040/T-044/T-046 unblocked and rescoped to the resolutions, T-041 rescoped (checkpoint respawn + XP penalty), new rows T-047 (pit falls - supersedes the pits-impassable rule), T-048 (enemy respawn rule), T-049 (registry-driven dev warp menu). Agent interpretations flagged for the next windowed pass: full-HP-on-respawn, suspended-vs-rebuilt room semantics for the respawn rule, fall damage (1 HP) and XP penalty (progress-to-next-level) as tunables | n/a (planning, docs only) | decisions locked; build ready to start at T-036 | `BLUEPRINT.md` (Design Decisions D-001 back-pointer + Phase 3 round row; Core Logic pits/death-respawn/enemy-respawn/save blocks rewritten; Data Model SaveData row); `TASKBOARD.md` (Pending Decisions resolved, lane rescoped, brief, this row) | Build sprint: T-036 first; Gameplan §12's stay-dead pattern deviation noted rather than Gameplan edited (BLUEPRINT owns current truth) |
| 2026-07-07 | Phase 3 planning | Claude | Gap analysis for Phase 3 (Data Model & Save/Load) on Kayden's go-ahead, sourced from Gameplan §3.1/§6/§12/§15 (M3.1-M3.3), the research audit's Resource-paradigm recommendation, `BLUEPRINT.md` Data Model, and the current code (SceneManager session scalars, string-id inventory, flags dict, class-hardcoded room construction). Output: 11 sequenced task rows (T-034..T-046, Ready lane, with dependency/blocker annotations and a suggested build order) + 6 open decisions with recommendations (D-006 save format, D-007 shield effect, D-008 respawn keep/lose rules, D-009 regular-enemy persistence, D-010 EncounterData/MapMeta timing, D-011 boot prompt). Notable gaps surfaced: GameState refactor must precede everything (one migration, not five); map ids/registry don't exist yet but the load flow needs them; `defeated_enemy_ids` (Gameplan) vs current respawn-regulars behavior is a real design conflict (D-009); Phase 3 triggers RUNBOOK's strict red/green policy for the new pure-logic code | n/a (planning, docs only) | plan written; decisions await Kayden | `TASKBOARD.md` (Pending Decisions Phase 3 round, Ready lane, Executive Brief, focus para, this row); `BLUEPRINT.md`/`RUNBOOK.md` deliberately untouched until decisions lock (they document what *is*, the taskboard holds what *might be*) | Kayden resolves D-006..D-011; then T-036 starts the build |
| 2026-07-07 | T-032 (round 1) | Kayden | First windowed playthrough of the Phase 2 build. Findings: (1) **B-05** - invisible collisions "all over the map... I don't see anything but grass" (root cause: forest Wall cells all drew the grass tile, zero tree tiles in the level); (2) wants more visible obstacles in the open stretch between spawn and the dungeon entry - "maybe not a maze, but at least trees or something"; (3) **B-06** - "the pressure plate never worked in the game," so he never reached the pit room; plate put ON HOLD by his call; (4) rescope to 4 rooms: hub brick wall (Oracle reference screenshot) where only some bricks push, chest moved to a side room behind a **locked door** ("I like having the door locked instead" of the chest), pit room = block pushing + ledge jumps, fight room's enemy drops the door key | Kayden's own play session + reference screenshot | round 1 verdict: rework required (built as T-033); forest look otherwise approved ("That looks good btw") | `TASKBOARD.md` this row + Bugs lane B-05/B-06 + T-032 re-scoped to round 2; `BLUEPRINT.md` Phase 2 Target revision block | Round 2 re-check after the T-033 rework |
| 2026-07-07 | T-033 | Claude | Playtest-feedback rework on `feature/phase2-tutorial-dungeon` (PR #6). Levels rebuilt by scratchpad patch script: `forest.ldtk` (tree tile on all 249 Wall cells incl. 8 new cluster trees; stray spawn-cell Pit removed), `tutorial_dungeon.ldtk` (hub: 13-brick wall y=8 with only (6,8) movable, no plate/chest, north dungeon_key door at (7,0), east gap doorway; new ChestRoom 7x7 level, unlocked shield chest; pit room: 1-wide ledges y=9/y=7 + 2-wide chasm y=3..4, block on the near bank at (3,5) - wedge-proof: no push can reach the ledges, every reachable sink is a solution). Code: `PushableBlock.movable` + post-import `Movable` field; `TutorialChestRoom`; hub/pit/fight room scripts + dialogue reworked; `dungeon_slime.tres` drops dungeon_key; dev overlay grants dungeon_key. Tests: solver rewritten (fixed bricks as obstacles, 1-cell jumps in reachability, hub goal = east+north+entry all reachable or lever, pit goal = every sink is in the chasm + exit always reachable); smoke test rewritten for the new flow incl. fixed-brick refusal, locked-door-without-key beat, and the chest-room leg. Verified (Godot 4.6.3): `--import` exit 0; unit tests PASS (17 suites / 103 tests / 351 checks); `main.tscn --quit-after 1` clean; smoke **PASS 109/109 on 5/5 consecutive runs**; windowed `screenshot_tour.tscn` run produced per-room PNGs, visually reviewed (trees where colliders are; brick wall, ledges+chasm, vault render correctly) | `cd game && Godot --headless --path . scenes/dev/slice_smoke_test.tscn` -> 109/109 (~60s); or `Godot --path . scenes/dev/screenshot_tour.tscn -- --out=/tmp/shots` for 5 PNGs | pass (headless + screenshot review); **Kayden's windowed re-check is T-032 round 2** | `BLUEPRINT.md` (status para, Phase 2 Target 2026-07-07 revision, Core Logic plate-hold + movable note, arch rows, Known Risks, 2 Design Decisions rows); `TASKBOARD.md` (focus, brief, T-032, B-05/B-06, T-033, this row); `RUNBOOK.md` (smoke/unit expectations, screenshot tour) | Plate mechanic parked - future latching/cue variant is an open design question; forest cluster density is a first pass, tune in round 2; screenshot tour needs a display (black under --headless) |
| 2026-07-06 | T-027 | Claude | **Phase 2 integration - the 3-room tutorial dungeon.** `tutorial_dungeon.ldtk` (3 levels) + thin room scripts: HubRoom 15x13 (entry door locks behind at the south doorway; plate at (5,5) = center of the 3x3 with the block at corner (4,4) and a 2-cell margin - the L-shaped push; visible locked chest; reset lever; plate-driven east door -> PitRoom; one-way west door), PitRoom 11x13 (2-wide pit spans the full width; block-fill + 1-cell jump crossing), FightRoom 11x9 (`dungeon_slime` key guardian, `unique_id` so it stays dead; west doorway loops to the hub via `SceneManager.exit_rooms(n)`, which repositions the player at the hub's west door and opens it for good). Chest -> shield (D-001) -> hub entry unbolts -> exit to the preserved forest. Rooms suspend going in, are freed/rebuilt backing out (escape valve); durable facts persist in flags. **Soft-lock proof:** `test_tutorial_softlock` BFS over every reachable (block, player-region) state of both real rooms - it caught a real wedge (block parked on the pit-room exit's only approach cell = trapped) which drove the doorway-approach no-block rule; both rooms now verify wedge-safe. Verified (Godot 4.6.3): `--import` exit 0; unit tests PASS (17 suites, 102 tests, 322 checks); `main.tscn --quit-after 3` boots clean; smoke test rewritten 47 -> **94 checks** covering the full dungeon loop + forced-defeat restart - **PASS 94/94 on 5/5 consecutive runs** (one earlier mid-battery timeout was a stale script-class cache from editing between runs, resolved by `--import`; not reproducible after) | `cd game && Godot --headless --path . scenes/dev/slice_smoke_test.tscn` -> 94/94 (~60s); windowed: beat the boss, enter the door, play the dungeon | pass (headless); **Kayden's windowed playthrough is the Phase 2 gate (T-032)** | `BLUEPRINT.md` Current Product Shape + scene contracts + Design Decisions (flagged interpretations) + Known Risks; `RUNBOOK.md` smoke expectation 94/94 + play loop + dev tools; `README.md` status para; `TASKBOARD.md` this row + lanes | Camera-zoom combat transition polish still deferred (Phase 4 framing); east-door/south-entry spatial mismatch between hub and pit room is door-to-door Zelda convention - flag if it reads wrong in play |
| 2026-07-08 | B-07 | Codex | Investigated movement path against T-021 history: `player.gd` still uses `GridActor.try_step` Tween movement and no later commit reverted the T-021 input state machine. Patched only the player timing knobs (`WALK_MOVE_TIME=0.16`, `MOVE_REPEAT_DELAY=0.17`) and updated `test_player_movement` to use the runtime move time plus a first-held-repeat hitch check. Could not run Godot here: no Godot executable is available on `PATH`, in Program Files, or in this repo; only `%LOCALAPPDATA%\\Godot` editor cache files were present. | Kayden windowed check: hold WASD/arrows in `main.tscn`, test tap-to-turn, tap-one-step, held walking, and key rollover | pending - static/code review only in this Windows session | `TASKBOARD.md` B-07 + proof row; no `BLUEPRINT.md`/`RUNBOOK.md` change because the grid-snap invariant and verification commands did not change | Run `cd game && Godot --headless --path . tests/run_tests.tscn`, then `Godot --headless --path . scenes/dev/slice_smoke_test.tscn`; windowed feel-check decides whether 0.16/0.17 is right or needs another small tune |
| 2026-07-08 | Asset planning | Codex | Added `docs/assets/ASSET_PLAN.md` with phase-timed asset batches A-I and `docs/assets/IMAGE_PROMPTS.md` as the required prompt ledger for generated images; added backlog tasks T-051..T-058 and cross-links from `BLUEPRINT.md`, `RUNBOOK.md`, and this documentation check table. Verified docs-only formatting with `git diff --check`. | n/a - docs-only planning artifact | pass (docs-only) | updated (`docs/assets/ASSET_PLAN.md`, `docs/assets/IMAGE_PROMPTS.md`, `BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`) | Actual image/audio generation not started; prompt log is empty until the first generated asset |
| 2026-07-08 | Blueprint framing | Codex | Folded Kayden's current project framing into `BLUEPRINT.md`: 2D Zelda-like adventure RPG with BG3-inspired party combat, explicit Core Promise, Design Pillars, compact hand-built world language, Dungeon Friends party progression, retro pixel-art/modern-flexibility target, and a no-literal-D&D clarification. Existing implementation/status sections were left intact. | n/a - docs-only framing update | pass (docs-only; `git diff --check`) | updated (`BLUEPRINT.md` top framing, Non-Goals, Phase 4 build-order line, Design Decisions row; `TASKBOARD.md` proof row) | No code or game behavior changed; this updates project direction language only |
| 2026-07-08 | T-051 | Codex | Started Asset Batch A with `game/assets/art/_scripts/generate_forest_overworld_tiles.py`, a deterministic Python source generator, and generated `game/assets/art/tilesets/forest_overworld_tiles.png` as a 4x4 16px forest overworld sheet: grass, path center/edge/corner, tree collision tile, shrub, rock, stump, cave mouth, campfire, sign, flowers, dirt variation, and shadow patch. Visual inspection of the PNG confirmed the sheet is nonblank and tile-readable at source scale | `game/assets/art/tilesets/forest_overworld_tiles.png` can be opened directly; runtime screenshot tour deferred until wired into LDtk | partial/pass for source-art generation; Godot import/runtime verification not run because Godot is not on PATH in this Windows session | updated (`docs/assets/ASSET_PLAN.md`, `TASKBOARD.md`); `IMAGE_PROMPTS.md` unchanged because no AI image prompt was used | Wire accepted tiles into the forest LDtk tileset after T-032 round-2 acceptance, run Godot import, screenshot tour, and T-020 display-scaling check |
| 2026-07-08 | T-052 | Codex | Started Asset Batch B with `game/assets/art/_scripts/generate_tutorial_dungeon_tiles.py`, a deterministic Python source generator, and generated `game/assets/art/tilesets/tutorial_dungeon_tiles.png` as a 4x4 16px tutorial dungeon sheet: floor, wall, fixed brick, loose brick, pit, ledge, filled pit, locked/open doors, closed/open chest, idle/used lever, cracked floor, and wall shadow. Visual inspection confirmed the PNG is nonblank and tile-readable at source scale | `game/assets/art/tilesets/tutorial_dungeon_tiles.png` can be opened directly; runtime screenshot tour deferred until wired into LDtk | partial/pass for source-art generation. PNG syntax/dimensions verified as valid 64x64 RGBA; Godot import/runtime verification not run because `Godot`/`godot`/`godot4` are still unavailable in this shell PATH | updated (`docs/assets/ASSET_PLAN.md`, `TASKBOARD.md`); `IMAGE_PROMPTS.md` unchanged because no AI image prompt was used | Import through Godot once the executable is available to this shell, then wire accepted tiles into `tutorial_dungeon.ldtk`, run screenshot tour and smoke test |
| 2026-07-08 | Taskboard-BLUEPRINT sync | Claude | Re-read `BLUEPRINT.md` in full and reconciled `TASKBOARD.md` against its 2026-07-08 updates (five product Design Pillars; tactics-RPG/BG3-turn-based combat identity, party of four, FE-visual-only; Gameplan.md retirement). Fixed drift: (1) Health bullet was stale two ways - claimed `feature/phase2-tutorial-dungeon` on Godot 4.6.3 with 17/103/351 counts, but PR #6 is merged and T-050 moved the project to 4.7; re-ran the real battery on `integration` @ `d8b6e79` with Godot 4.7.stable: `--import` exit 0, unit `PASS 18 suites/109 tests/371 checks`, smoke `PASS 109/109`, `main.tscn --quit-after 1` clean - also closing B-07's pending headless half (windowed feel check still Kayden's); (2) added a Design update (2026-07-08) brief bullet mirroring the BLUEPRINT combat-identity/pillars row; (3) live-lane references to the retired Gameplan (T-020/T-037/T-039/T-043/T-048, Backlog+Deferred intros, gap-analysis note) now say "retired" and point at BLUEPRINT owners - proof-log rows left verbatim per append-only; (4) D-005 resolution annotated with the 2026-07-08 model sharpening; (5) T-035/T-043 gained the don't-preclude note for the future move/attack-range fields (BLUEPRINT Data Model note). No Ready-lane scope changes - Phase 3 build order and D-006..D-011 resolutions all still consistent with BLUEPRINT. Observed noise: one "Lambda capture at index 0 was freed" ERROR line mid-smoke-run (test still 109/109, exit 0) - not previously logged, flagged for a look if it recurs | n/a (docs sync + verification re-run) | pass | updated (`TASKBOARD.md` only; `BLUEPRINT.md` needed no edits - it is the source being synced from; `RUNBOOK.md`/`README.md`/`AGENTS.md` checked, no update needed) | Kayden-side gates unchanged (T-032 round 2, T-020, B-07 windowed feel); the smoke-run lambda-capture warning is unowned - promote to a bug row if it reproduces |
| 2026-07-08 | T-054 | Codex | Started Asset Batch D with `game/assets/art/_scripts/generate_item_save_icons.py`, a deterministic Python source generator, and generated `game/assets/art/icons/item_save_icons.png` as an 8-icon 16px sheet: forest_key, dungeon_key, shield, save crystal, empty/selected inventory slots, Continue badge, and New Game badge. Imported through `E:\Godot\godot.cmd` on Godot 4.7, which created `game/assets/art/icons/item_save_icons.png.import`; the same import pass also picked up the previously unimported `tutorial_dungeon_tiles.png.import` | `game/assets/art/icons/item_save_icons.png` can be opened directly; Godot import metadata exists at `game/assets/art/icons/item_save_icons.png.import` | pass for source-art generation and Godot import. PNG syntax/dimensions verified as valid 64x32 RGBA; `E:\Godot\godot.cmd --headless --path . --import` exited 0; `E:\Godot\godot.cmd --headless --path . scenes/main.tscn --quit-after 1` exited 0 with `SceneManager ready.` | updated (`docs/assets/ASSET_PLAN.md`, `TASKBOARD.md`); `IMAGE_PROMPTS.md` unchanged because no AI image prompt was used | Wire accepted icons into `ItemData`, save crystal, inventory/HUD, and boot-prompt UI when those Phase 3 systems land |
| 2026-07-08 | T-020/T-032 (round 2) | Kayden | Kayden's windowed pass: "I have checked the game and it works fine" - **Phase 2 accepted** (T-032 round 2) and the 3-resolution display check closed (T-020; `expand` kept, no letterbox fallback). Two feedback items: (1) "The block movement is a bit chunky" - accepted for now, parked as Backlog T-059 (ambiguous between block-push and grid-step feel; investigate both there); (2) "my main complaint right now is combat, which is phase 4. So I think that is a good next place to work" - drove the Phase 4 pull-forward below | Kayden's own play session | pass - Phase 2 done, Phase 1 fully done | `TASKBOARD.md` (T-032/T-020 -> done, B-07 closed, T-059 added, brief/focus); `BLUEPRINT.md` (status para, current phase, ultrawide risk row) | none for Phase 2; T-059 waits behind combat |
| 2026-07-08 | Phase 4 planning | Claude | Gap analysis for Phase 4 (Combat MVP) on Kayden's explicit pull-forward, sourced from `BLUEPRINT.md` (Party And Combat Model, Core Logic combat rule, 2026-07-08 tactics-RPG identity) and the current `combat.gd` walking skeleton (1v1, menu-driven, placeholder formulas, fade transition). Output: 10 new rows (T-060 combat math red/green, T-061 TurnManager/two-layer FSM, T-062 multi-unit parties + arena, T-063 selection + range highlighting incl. the flagged range-field shape decision, T-064 Attack/Ability/Item/Defend, T-065 zoom transition, T-066 encounter/reward wiring, T-067 combat HUD, T-068 headless acceptance battery, T-069 Kayden windowed acceptance) + 2 open decisions with recommendations (D-012 arena source - local-terrain seed recommended; D-013 temporary test companion - recommended yes). Phase 3's M3.1 data half (T-034/T-035/T-043/T-044/T-045/T-046) folded into the Phase 4 build order as prerequisites; the M3.2/M3.3 save/load half parked behind combat - **a build-order re-sequencing on Kayden's instruction, flagged in BLUEPRINT Design Decisions, not silent drift** | n/a (planning, docs only) | plan written; build starts at T-034 on `feature/phase4-combat-mvp` | `TASKBOARD.md` (focus, brief, Pending Decisions Phase 4 round, lane intro + rows, this row); `BLUEPRINT.md` (current phase, Design Decisions row) | D-012/D-013 need Kayden's verdict before T-062; combat-math formula numbers are first-cut tunables until T-069 |
| 2026-07-08 | Phase 4 build sprint (T-035/T-043/T-044/T-045/T-046/T-060/T-061/T-062/T-063/T-064/T-068-core) | Claude | Kayden resolved D-012 (local-terrain arena) and D-013 (test companion) and said "start phase 4 and get as far as you can." Built on `feature/phase4-combat-mvp` in three commits: **(1) data** - AbilityData + strike/mend/potion .tres, EncounterData/MapMeta stubs + samples, Progression XP curve (red/green), stat classes gain move_range/attack_range/portrait slots + typed ability arrays, `companion_test.tres` ("Buddy") joins the default roster, GameState gains party_mp; **(2) combat core** - CombatScene rebuilt as the BG3-model tactical battle: two-layer FSM + TurnManager (red/green: interleaved initiative, deterministic ties, mid-round death skips), CombatMath extracted with ability power + heals (red/green), D-012 arena seeded from the room's cells around the contact point and restricted to the contact-connected region (a roaming slime met in a tree pocket stalemated an early smoke run - the region restriction fixes it, plus a loud 100-round safety valve), FE-style move highlights + cursor + threat fringe, Attack/Ability/Item/Defend (MP costs, potion stock, D-007 shield gate), close-and-attack AI, party HP/MP payload written back to GameState; **(3) T-068 core** - `test_combat_scene` (27 checks incl. a seeded 2v2 auto-battle) + `autostart` test hook. Interpretations flagged: "Wait" as the fifth always-available menu entry; support abilities (mend/potion) land without a d10 roll; potions have no natural source yet (dev grants only). **Verified (Godot 4.7, foreground from `game/`): `--import` clean; unit 22 suites / 137 tests / 483 checks PASS; smoke 109/109 on 5/5 consecutive runs post-connectivity-fix + 1 more after final test hooks; `main.tscn` boots clean** | `cd game && Godot --headless --path . tests/run_tests.tscn` (~8s); windowed: walk into any slime - the battle happens on the terrain where you stood, hero + Buddy vs the enemy | pass (headless); **T-069 windowed pass is Kayden's - playable now** | `BLUEPRINT.md` (combat scene contract rebuilt, D-012/D-013 decision row, Data Model status); `RUNBOOK.md` (suite list + counts); `TASKBOARD.md` (lane statuses, focus, this row) | T-065 zoom transition (fade still in place), T-066 LDtk encounter-field wiring (combat side ready), T-067 HUD strip; combat numbers all tunables pending T-069; the pre-existing "Lambda capture freed" smoke noise traces to `slice_smoke_test._until`, unowned |
| 2026-07-08 | T-034 | Claude | ItemData + ItemLibrary + `{id: qty}` inventory on `feature/phase4-combat-mvp`. New `item_data.gd` (id/display_name/description/ItemType enum/stretch fields + `stacks()`), `item_library.gd` (dir-scan id->ItemData cache, `.remap`-tolerant for exports, `display_name()` capitalize fallback, `register()` for tests/dynamic content), `game/data/items/{forest_key,dungeon_key,shield}.tres`. `GameState.inventory` -> `{id: qty}` Dictionary; `SceneManager.add_item(item, qty)` keeps the key/gear dedup rule and stacks consumables, new `remove_item()` (refuse-if-insufficient, erase-at-zero) and `inventory_summary()` (display names + "x qty" suffix - HUD and F1 overlay both use it); chest/door/victory-banner text reads ItemData display names. Red/green: `test_item_data` (6 tests) written first; true red observed as one real failure (banner test expected raw id - updated to expect "Forest Key"); negative control (corrupt qty expectation -> suite FAIL) then restored. **Verified (Godot 4.7, foreground, from `game/`): `--import` clean, unit PASS 19 suites / 115 tests / 391 checks, smoke PASS 109/109, `main.tscn` boots clean.** Incident during verification: three consecutive "hangs" (import + test runs) were NOT code - the shell's persistent cwd had moved to the repo root by an earlier `cd` for git, so `--path .` pointed at a directory with no project.godot, the exact documented T-022 RUNBOOK gotcha; killed the stray processes, re-ran from `game/`, all green. Collateral hardening while misdiagnosing: `run_tests.gd` now guards `can_instantiate()` so a suite that loads-but-fails-to-compile fails the run instead of crashing `_ready()` before `quit()` (a real hang class, kept) | `cd game && Godot --headless --path . tests/run_tests.tscn` (~3s) then the smoke test; windowed: open a chest, check the HUD reads "Shield" not "shield" | pass (headless) | `BLUEPRINT.md` Data Model status para (ItemData built); `RUNBOOK.md` unit-test counts + suite list (also backfilled the missing `test_game_state` entry); `TASKBOARD.md` (T-034 done, Done lane, this row) | `potion.tres`/first consumable arrives with T-064; ItemData icons wire in at Asset Batch D (T-054); windowed HUD eyeball is bundled into the next Kayden pass (no dedicated gate) |
| 2026-07-09 | Harness docs Phase 4 sync | Codex | Reconciled the live control-doc surfaces against `feature/phase4-combat-mvp`: D-012 local-contact terrain and D-013 temporary Buddy companion are resolved/reaffirmed; the Executive Brief and Ready order now point to T-066 -> T-065/T-067 -> T-068 -> T-069; BLUEPRINT data/range/formula/test status matches the implemented core; RUNBOOK controls, test policy, and T-069 checklist match current behavior; README and AGENTS no longer describe the old 1v1/no-unit-suite state. Historical Proof Log rows were preserved verbatim. Fresh same-day baseline: `--import` exit 0, unit PASS 22 suites / 137 tests / 483 checks, `main.tscn` boot exit 0 | n/a - docs-only; one-glance status is the demo | pass (`git diff --check` + stale-phrase consistency scan) | updated (`AGENTS.md`, `BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, `README.md`); `CLAUDE.md` checked, no update needed because it correctly delegates shared rules to AGENTS | T-066 is the next gameplay task; Phase 4 branch still needs final tasks, T-069 acceptance, and landing into `integration` |
| 2026-07-09 | T-065/T-066/T-067/T-068 | Codex | Completed the non-review Phase 4 lane on `feature/phase4-combat-mvp`: LDtk `EncounterId` loads `forest_pair` for regular enemies, combat builds its two-enemy party, and victory sums the whole group’s XP/loot; player-camera zooms 2.2x before the existing short hand-off fade and restores after combat; the combat CanvasLayer now shows live TurnManager order, party HP/MP, action prompts, and damage/heal popups. Added red/green coverage for group rewards, LDtk encounter adoption, and HUD order/death removal; extended the smoke test through an actual two-enemy forest fight and 10-XP reward. Visual live-game pass confirmed the HUD in a 2v2 fight. | `cd game && Godot --headless --path . tests/run_tests.tscn`; `scenes/dev/slice_smoke_test.tscn`; windowed `main.tscn` encounter | pass - import clean; units **22 suites / 139 tests / 489 checks**; smoke **111/111 on 5/5 consecutive runs**; visual HUD pass | updated (`TASKBOARD.md`, `BLUEPRINT.md`, `RUNBOOK.md`, `README.md`); `AGENTS.md`/`CLAUDE.md` checked, no update needed | T-069 remains Kayden’s windowed balance/feel gate. The recurring pre-existing smoke warning `Lambda capture at index 0 was freed` still appears during the forced-restart leg but does not fail the 5/5 battery; it remains unowned pending recurrence triage |
| 2026-07-09 | Phase 4 gap-closure pass (T-069 prep) | Claude | Re-verified and landed the Phase 4 tail: committed T-065/T-066/T-067 + doc sync (`0039fa8`), added F1-overlay `P` = grant 3 potions so T-069 can exercise the Item command (unit `test_grant_potions_stacks_quantity`), fixed the restart-leg `Lambda capture was freed` warning by capturing the old room's instance id in `slice_smoke_test.gd`, trimmed the proof log to 30 rows (oldest 15 verbatim in `TASKBOARD_ARCHIVE.md`), merged `feature/phase4-combat-mvp` into `integration` and pushed both | `cd game && Godot --headless --path . tests/run_tests.tscn` then `scenes/dev/slice_smoke_test.tscn` | pass - `--import` exit 0; `main.tscn` boot clean (`SceneManager ready.`); unit **22 suites / 140 tests / 490 checks, 0 failed**; smoke **111/111 x5 across the pass**, freed-lambda warning gone | updated (`TASKBOARD.md`, `BLUEPRINT.md`, `RUNBOOK.md`); `README.md`/`AGENTS.md`/`CLAUDE.md` checked, no update needed | T-069 (Kayden's windowed acceptance) is the only remaining Phase 4 row; consumables' in-world source and the "Wait" command interpretation await his verdict |
| 2026-07-10 | Phase 3 save/load lane (T-037/T-038/T-039/T-040/T-041/T-042/T-047/T-048/T-049) | Claude | Kayden's explicit "resume the parked tasks" request un-parked the M3.2/M3.3 lane; built in 9 commits on `feature/phase3-saveload` (off `integration`), strict red/green for every pure-logic piece. **(1) T-037** SaveData (RefCounted, NOT a Resource - JSON from `user://` can never embed scripts) + SaveManager statics: atomic temp+rename writes, tolerant loads (missing/corrupt -> null + warning), 3 slots, JSON-float re-inting; test-pointable `SceneManager.save_dir` so no automated run can clobber a real save. **(2) T-038** MapRegistry: 5 stable ids -> factories, `id_for()` reverses live rooms; boot/restart resolve through it (class refs aren't const expressions - `_entries()` is a static func). **(3) T-048** enemies always respawn on rebuild: suppression + `defeated_*` flags removed, fight room's dead quiet-chamber branch dropped, smoke asserts NO stay-dead flags. **(4) T-039** SaveCrystal entity (post-import + adoption + contract line) at (21,14) beside the healer in `forest.ldtk` (def + instance added via script, uid 101); interact -> `save_game(1)` -> confirm dialogue (*interpretation:* confirm-after-write, no choice UI pre-Phase 6). **(5) T-040** `load_game()`: registry room + `spawn_override` + wholesale GameState swap, failed loads leave the session untouched; BootPrompt (E/Space Continue, X/Esc New Game) only when a save exists; verified rooms rebuild door/chest state from flags (not assumed). **(6) T-041** checkpoint respawns: `Progression.xp_after_defeat` floor clamp (`DEFEAT_XP_LOSS=1.0` tunable), full-HP respawn (flagged), dungeon defeat -> FRESH hub with the suspended forest kept beneath, outside -> beside the healer; restart-from-beginning retired (dev spikes keep it; `restart_game()` stays for the overlay). **(7) T-047** pit falls: Player-only `try_step` override, shrink-drop tween, `FALL_DAMAGE=1`, respawn at `entry_cell` (stamped at spawn + every room restore), fatal falls chain to defeat; enemies/pathing unchanged. **(8) T-049** F1 warps 1-5 = the registry (reset -> R). **(9) T-042** two-process battery `saveload_battery.tscn`: real chest/door/crystal interacts -> save; relaunch answers the boot prompt via a real confirm input -> position/flags/inventory/door/chest all survive quit+relaunch. Smoke reworked: crystal-save leg, ledge-fall leg, both defeat legs, final load-rollback leg | `cd game && Godot --headless --path . scenes/dev/saveload_battery.tscn -- --phase=save && Godot --headless --path . scenes/dev/saveload_battery.tscn -- --phase=load` (<1 min, prints 10/10 then 11/11 PASS) | pass - `--import` 0 script errors; `main.tscn` boots (`SceneManager ready.`); unit **27 suites / 169 tests / 615 checks, 0 failed**; smoke **136/136 on 5/5 consecutive runs**; battery save 10/10 + load 11/11, both exit 0 | updated (`TASKBOARD.md` focus/brief/lanes/rows/this row; `BLUEPRINT.md` death-respawn/D-009/save/SaveData rows -> built, phase position; `RUNBOOK.md` play loop + save location/wipe, battery section, suite list, counts, troubleshooting rows); `README.md`/`AGENTS.md`/`CLAUDE.md` checked, no update needed | Windowed checks fold into T-069 (boot prompt, crystal, pit-fall feel, both respawns, F1 warps); tunables awaiting that pass: `DEFEAT_XP_LOSS`, `FALL_DAMAGE`/`FALL_TIME`, full-HP respawn; multi-slot save UI stays non-MVP (slots exist in the format only) |
| 2026-07-10 | Phase 3 tuning round (D-014..D-017, T-070/T-071 spawned) | Claude | Kayden pre-answered the save/load lane's flagged tunables ahead of his PC playtest; applied directly on `integration` (small explicit-decision changes): **D-014** defeat XP penalty 1.0 -> **0.25 of above-floor progress** (`Progression.DEFEAT_XP_LOSS`); **D-015** respawn HP full -> **80% of max** (`SceneManager.RESPAWN_HP_FRACTION`, new `restore_party_after_defeat()`; MP still full - flagged, no MP economy yet); **D-016** pit fall flat 1 HP -> **10% of max HP floored at 1** (`Player.FALL_DAMAGE_FRACTION` + `fall_damage()`); **D-017 partial** - mini-bosses now drop potions with their keys (boss_slime: +2, dungeon_slime/key_guardian: +1) since end-of-battle total collection is already the reward model; regular-enemy food/potion drops need a drop-chance model (new T-070, ready, after T-069) and shops/treasure ride future currency (new T-071, Backlog). Save-confirmation dialogue (his item 4) already existed since T-039 - no change needed. Unit/smoke expectations updated to the tuned numbers | `cd game && Godot --headless --path . tests/run_tests.tscn` then the smoke test; windowed: die once - respawn at 16/20 HP with ~25% of in-level XP gone; fall in the pit room - 2 HP | pass - unit **27 suites / 169 tests / 619 checks, 0 failed**; smoke **136/136 x3**; saveload battery 10/10 + 11/11; `main.tscn` boots clean | updated (`TASKBOARD.md` D-014..D-017 table, T-070/T-071 rows, T-041/T-047/T-069 status notes, header, this row; `BLUEPRINT.md` death-respawn/pits rules + Design Decisions row; `RUNBOOK.md` play loop + counts) | Kayden judges the 25%/80%/10% numbers in the PC playtest; T-070's drop-chance model is the next loot step; his PC-side in-tandem changes (if any) should be pushed/pulled before diverging further |
| 2026-07-09 | World lore story spine | Codex | Added `docs/WORLD_LORE.md` as the ongoing story document for the regional village -> dungeon -> item loop, captured the current Forest -> River Valley -> Mountain -> City -> Dragon Lair arc, and recorded the four working-name legendary items for the dragon quest. `BLUEPRINT.md` links to the story doc and summarizes the revised world/story spine without changing implementation scope. | n/a - docs-only story artifact | pass (docs-only; `git diff --check`) | updated (`docs/WORLD_LORE.md`, `BLUEPRINT.md`, `TASKBOARD.md`) | Item names and region-to-item assignments remain working placeholders; no code or game behavior changed. |
| 2026-07-10 | T-069 windowed feedback recut | Codex | Applied Kayden's completed playtest feedback: combat arena 9x5 -> 17x7 near-full-screen while retaining local terrain; three clear deployment columns per side plus centered vertical formations prevent Hero/Buddy turn-one trapping; cancel/stay X -> Q; jump Alt -> Space with C fallback and E-only overworld interaction; pit fall 10%-max Hero-only -> flat 10 HP party-wide so Buddy carries hazard damage into combat; reset lever retained as the block soft-lock escape valve but copy now calls it optional and explains exactly what it resets. Vertical deployment also prevents the observed adjacent-unit label overlap. | `game/scenes/main.tscn`; enter any forest fight, then intentionally fall before the dungeon guardian | pass (headless): red observed at 6 expected failures; green unit 27 suites / 171 tests / 626 checks; slice smoke 137/137 after adding Buddy hazard persistence; `main.tscn` boot clean. Windowed arena feel remains Kayden's gate. | updated (`BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, `README.md`) | Re-check the wider arena, Q/Space controls, one pit fall before the guardian, and lever copy; no commit/push performed. |
| 2026-07-10 | D-018 / authored weighted arena planning | Codex | Kayden's second windowed pass proved the enlarged literal-terrain arena could still be unfightable. Reframed D-012 around its intended product outcome (biome/context fidelity), then planned D-018 and T-072..T-075: deterministic weighted shuffle bag; 2 empty / 3 mid / 2 hard editable LDtk forest templates; context/contact-oriented deployment; variable enemy groups independent of arena tier; safety validator and gallery preview. | Taskboard lane is the review artifact | docs-only planning pass (`git diff --check`) | updated (`TASKBOARD.md`, `BLUEPRINT.md`) | Implement T-072 first; exact art/terrain dressing remains editable content, while weights begin at 5/2/1 per template and stay tunable. |
| 2026-07-10 | LDtk editor-project repair | Codex | Repaired all four `.ldtk` sources as complete LDtk 1.5.3 editor projects: restored missing tileset `savedSelections` arrays, corrected entity render mode `Rect` -> `Rectangle`, generated the missing entity custom-field definitions with stable UIDs, and linked/populated every placed field instance so existing NPC, enemy, door, chest, and puzzle values remain editable. | Open `game/assets/levels/forest.ldtk` in LDtk 1.5.3; the other three projects use the same repaired structure. | pass - official LDtk 1.5.3 JSON schema reports 0 errors for each file; Godot 4.7 `--import` reimported all four successfully; `main.tscn --quit-after 1` exited 0 with `SceneManager ready.` | `TASKBOARD.md` proof row; `BLUEPRINT.md`/`RUNBOOK.md` checked, no update needed because the architecture and procedures were unchanged | Kayden's direct LDtk GUI open/edit/save check remains; generated Godot import artifacts were refreshed but already had pre-existing working-tree changes. |
| 2026-07-10 | Forest slime patrol tuning | Codex | Set all seven regular `forest_slime` LDtk instances to `LeashRadius = 3` and made 3 the LDtk field default for newly placed Enemy entities; retained the boss's authored radius of 2. Confirmed the existing shared-archetype model: `StatsId` selects an `EnemyStats` `.tres`, so balance and presentation fields remain centralized per slime variant. Updated the full-slice smoke navigation after Kayden's forest redesign invalidated two hard-coded approach assumptions: red observed at 8 expected layout/save-chain failures, then green via live-wall and reachable-neighbor checks. | Run `game/scenes/main.tscn` and observe regular slimes patrol within three Manhattan cells of their spawn. | pass - all four projects pass the official LDtk 1.5.3 schema; Godot 4.7 reimported `forest.ldtk`; generated `Forest.scn` contains radius 3 on all seven regular slimes; unit **27 suites / 171 tests / 626 checks**; smoke **137/137**; main boot exited 0 with `SceneManager ready.` | updated (`BLUEPRINT.md` enemy-archetype + transitional LDtk authoring contracts, `TASKBOARD.md` proof); `RUNBOOK.md` checked, no update needed because commands/counts are unchanged | Windowed patrol-feel check remains; future elemental/tier variants need new named `EnemyStats` resources and corresponding LDtk `StatsId` values. |
| 2026-07-10 | Locked keyboard scheme restoration | Codex | Restored Kayden's T-051 keyboard decision from commit `8020658`: E only interacts, Enter only confirms, Escape only cancels/backs out, Tab opens the menu, and Space only jumps. Removed the later C jump, Space confirm, and Q cancel fallbacks; corrected the pit tutorial and current player-facing docs. Added an exact InputMap regression test so fallback bindings cannot silently return. | Run `game/scenes/main.tscn`; verify E interacts, Space jumps without advancing dialogue, Enter confirms, Escape cancels, and C/Q do nothing. | pass - red observed at exactly 3 expected binding failures; `--import` exit 0; unit **27 suites / 171 tests / 627 checks, 0 failed**; smoke **137/137**; `main.tscn --quit-after 1` exited 0 with `SceneManager ready.` | updated (`BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, `README.md`, pit tutorial); `docs/WORLD_LORE.md` checked, no update needed because input bindings do not change lore | Windowed key-feel check remains; unrelated pre-existing Godot import-file changes were preserved. |
| 2026-07-10 | D-019 keyboard/controller 1:1 anchor | Codex | Replaced the intermediate Enter/Escape-only scheme with Kayden's controller-first anchor: WASD/D-pad movement; E/A interact+confirm; Space/B jump; Q/X cancel/back; F/Y `character_menu`; Tab/Start broader menu. Added real joypad bindings to every active action and a named F/Y action now; the character-menu UI itself is tracked as T-076 for Phase 5. Updated dialogue/boot prompts and control docs. | Connect a controller and run `game/scenes/main.tscn`; verify the five keyboard/controller pairs behave identically. F/Y is expected to do nothing visible until T-076. | pass - red observed at 7 expected missing/mismatched bindings; `--import` exit 0; unit **27 suites / 171 tests / 635 checks, 0 failed**; smoke **138/138**; `main.tscn --quit-after 1` exited 0 with `SceneManager ready.` | updated (`game/project.godot`, input regression test, smoke action contract, dialogue/boot prompts, `BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`); `README.md` and `docs/WORLD_LORE.md` checked, no update needed | Physical controller play-check remains; analog-stick movement is not part of this anchor, only D-pad. |
| 2026-07-10 | Latest playthrough planning recut (D-019..D-023, T-077..T-079) | Codex | Recorded Kayden's latest verdicts: current prompts show keyboard keys only while controller bindings remain active; controller glyphs are a later coherent UI pass; teammate collision remains intentional positioning balance; Phase 4 Item use must identify the consumable; manual jumping is benched behind a future Zelda-style traversal item; and lever + pressure plate + pushable blocks become the first puzzle set. Reopened B-06 under T-078 and set the acceptance order to authored arenas -> readable Item -> tutorial recut -> T-069 -> Phase 5. Updated the existing dialogue/boot/main labels to keyboard-only text without changing bindings or puzzle/combat behavior. | Read the Current focus, D-019..D-023, and T-077..T-079; the taskboard itself is the under-one-minute review artifact. | pass - docs/UI-copy only; `git diff --check`; stale-current-phrase scan; Godot 4.7 `--import` and `main.tscn --quit-after 1` both exit 0 with `SceneManager ready.`; no puzzle/combat verification claimed because those changes are planned, not implemented | updated (`BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, keyboard prompt copy); `README.md` checked, no update needed until T-078 changes the playable route; `docs/WORLD_LORE.md` checked, no update needed because no story decision changed | T-072 is still the highest-priority ready implementation task; T-078 must not be marked done until the dungeon is completable without invoking jump and Kayden windowed-accepts the new puzzle read. |
| 2026-07-10 | Generated sprite source intake | Kayden + Codex | Added Kayden's three generated source sheets (armored knight, wizard, red ooze) under `game/assets/art/sprites/` and created PROMPT-001..003 provenance records. Inspected the actual files before acceptance: all are valid RGB PNGs, but the checkerboard is baked into the pixels, there is no alpha channel, frame boxes are irregular, and the originating prompts/model settings were not available in this task. They are intentionally recorded as concept/source sheets, not runtime SpriteFrames. | Open the three PNGs directly under `game/assets/art/sprites/`. | pass for repository intake and file validation (`file`, `sips`, `git diff --check`); runtime animation not claimed | updated (`docs/assets/IMAGE_PROMPTS.md`, `docs/assets/ASSET_PLAN.md`, `TASKBOARD.md`); `BLUEPRINT.md`/`RUNBOOK.md` checked, no update needed because no runtime behavior or procedure changed | Recover the exact prompts/model settings; assign knight/wizard roles; remove baked backgrounds; crop/normalize transparent frames; then wire and windowed-check them under T-053/T-055/T-056. |
| 2026-07-10 | T-077/T-078 | Codex | Shipped a named combat-consumable chooser (display name, quantity, acting unit, Back, selected-id-only consumption) and recut the LDtk tutorial pit room into a no-required-jump mechanism lesson: visible latching lever gate, movable block on a momentary pressure plate, second held-open gate, reset lever, and optional pit hazards. Hardened the smoke harness to clear only its scratch save before boot so interrupted runs cannot stall on Continue. | Under-one-minute route: F1 -> warp to Tutorial Hub, enter east mechanism room, E-toggle bronze lever, push block right twice onto silver plate, walk north through the held gate. In combat, grant potions with F1/P and choose Item. | pass - Godot 4.7 import; main boot; strict red/green unit **27 suites / 174 tests / 653 checks**; exhaustive mechanism-room state solver; full slice smoke **135/135** | updated (`BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, `README.md`) | T-069 windowed readability/feel judgment remains; it is blocked only by T-072..T-075 authored arenas. |
| 2026-07-10 | T-078 screenshot-feedback recut | Codex | Superseded the first T-078 room layout after Kayden's screenshots showed the dark pit bands made floor unreadable and the latch-plus-plate sequence obscured the lesson. Removed every Pit cell and every leftover visual pit-row gap; removed the latching lever/gate; rebuilt the room as one continuous floor with one flush brass pressure plate, one heavy block, one plate-driven north gate, and one reset lever. Player weight opens the gate, stepping off closes it, and the block holds it open. Also removed the stale `Space: jump` HUD advertisement and made the screenshot tour use scratch saves so real Continue data cannot cover captures. | Windowed `scenes/dev/screenshot_tour.tscn` rendered `game/.godot/t078_preview/pit.png`: continuous floor, brass plate visible, block/reset lever separated. Play route: enter east room, step on/off lower-left plate, then stand right of the block and push it left three cells onto the plate; walk through the north gate. | pass - red observed at 9 shipped-room contract failures; LDtk JSON parses with 0 duplicate IIDs and 0 Pit cells; Godot 4.7 reimport; unit **27 suites / 174 tests / 633 checks**; exhaustive recovery solver; full slice smoke **134/134**; rendered screenshot inspected | updated (`BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, `README.md`); `docs/WORLD_LORE.md` checked, no update needed because puzzle presentation does not alter lore | Kayden windowed readability re-check rides T-069; T-072..T-075 authored arenas remain the active blockers. |
| 2026-07-11 | Kenney test-library intake | Kayden + Codex | Added six original Kenney pixel-art packs under `game/assets/kenney/` for UI, input-prompt, dungeon, interior, character, and general RPG prototyping. Preserved the original pack layouts, previews, tilemaps, and per-pack CC0 licenses; added a local catalog with runtime-promotion rules. | Open `game/assets/kenney/README.md`, then inspect each pack's `Preview.png`. | pass - inventoried **898 files / 1,522,635 bytes**; confirmed six included `License.txt` files identify the packs as CC0; sensitive/binary-package scan found no executables, archives, Android keystores, or signing material; Godot 4.7 import exit 0; unit **27 suites / 174 tests / 633 checks**; full slice smoke **134/134**; main boot exit 0 with `SceneManager ready.`; runtime use not claimed | updated (`game/assets/kenney/README.md`, `docs/assets/ASSET_PLAN.md`, `TASKBOARD.md`); `BLUEPRINT.md`/`RUNBOOK.md`/`README.md` checked, no additional update needed because the intake does not change runtime behavior, architecture, setup, or public controls | Packs are test/reference inputs only until individual assets are selected, copied into established runtime paths, wired, and windowed-verified through T-052/T-053/T-055/T-056/T-079. |
| 2026-07-10 | T-053/T-055 first runtime sprite pass | Kayden + Codex | Turned the three approved concepts into production-structured four-frame idle strips: Hero knight, temporary Buddy wizard, and red ooze. Image edits preserved each identity in one whole-strip pass; `clean_generated_strip.py` removes only edge-connected checkerboard pixels so white armor/beard detail survives; the shared normalization pass produces 128px transparent frames with one scale and bottom-center anchor. Added SpriteFrames resources, data-driven CharacterStats/EnemyStats wiring, AnimatedSprite2D rendering in overworld and combat, boss scaling, outward team labels, a permanent combat showcase scene, refreshed README screenshots, and exact PROMPT-004..006 provenance. Red observed in `test_data_resources` before the resource/CombatUnit wiring; green after implementation. | `/Applications/Godot.app/Contents/MacOS/Godot --path . scenes/dev/runtime_sprite_showcase.tscn --resolution 1280x720 -- --out=/tmp/dungeon-runtime-sprites.png`; preview sheet at `docs/assets/previews/runtime_sprite_preview.png` | pass - real-alpha/frame-bound inspection; Godot 4.7 import; unit **27 suites / 173 tests / 644 checks**; main boot; slice smoke **138/138**; windowed screenshot tour (5 rooms) and combat showcase visually inspected at 1280x720 | updated (`BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, `README.md`, `docs/assets/ASSET_PLAN.md`, `docs/assets/IMAGE_PROMPTS.md`) | This is a deliberately high-impact first pass, not full art completion: NPC/object/environment blocks, directional overworld frames, attack/defend/ability effects, distinct slime palettes/silhouettes, and T-028 aggro art remain. |
| 2026-07-11 | QA audit B-08..B-21 logged; quick wins patched (B-08..B-14, B-16, B-18..B-20) | Claude (Cowork session) | Front-end-first code audit of the fresh-install player path found 14 issues, logged as B-08..B-21. Patched same-session: overworld actors reverted to colored placeholder bodies per Kayden (B-08, `USE_SPRITE_BODY := false` - restores the facing marker; flip to true after the Mac Mini asset merge); combat continue label E-only (B-09); dead-at-build units hidden + DOWN rows (B-10); combat view derives from `get_viewport_rect().size` instead of literal 1280x720 (B-11); enemies frozen and `start_encounter` refused during room transitions (B-12); root-menu Q no longer ends the turn (B-13); failed Continue-load now messages the player (B-14); roster-wide HP HUD (B-16); save-failure copy reworded (B-18); dialogue end-of-text hint (B-19); `unique_id` docstring aligned with D-009 (B-20). B-15/B-17 left `ready`; B-21 `deferred`. | Read the B-08..B-21 rows in Bugs; then windowed: boot fresh, talk to the NPC (facing marker back), fight twice with Buddy downed, resize to non-16:9 before a fight, park a slime at the dungeon doorway and step through | partial - edits made on the working tree of `codex/playtest-input-puzzle-plan` (already dirty; nothing committed). gdtoolkit parse: 5/7 edited scripts pass; `scene_manager.gd`/`overworld_enemy.gd` could not be parsed from the audit sandbox because its file mirror served truncated copies (Windows-side files verified complete by direct read). **No Godot available in the audit environment - the RUNBOOK headless battery (import, unit suites, slice smoke, saveload battery) and all windowed checks still need to run on Kayden's machine before any of these move to closed.** | updated (`TASKBOARD.md`; code comments carry the B-ids). `BLUEPRINT.md`/`RUNBOOK.md`/`README.md` checked, no update needed: no locked decision, verification procedure, or public control changed - B-08 is an explicitly temporary art toggle | Run the RUNBOOK battery + windowed verify of the patched rows; decide B-10's revive rule and B-15's overwrite-confirm shape; B-17 rides the T-069/Phase 5 combat-UI pass |
| 2026-07-11 | B-08 rev 2: beta facing affordance over idle-only Kenney sprites | Claude (Cowork session) | With sprites re-enabled by the Kenney merge, facing needed a read that doesn't disturb the art: (1) `GridActor.set_facing` flips `AnimatedSprite2D` bodies horizontally on sideways facing (no-op on front-facing art; comparison assumes right-facing native art - invert if the sheet faces left); (2) the player gets a small translucent chevron (`Polygon2D`, z 1, under the sprite at z 2) hugging the faced cell edge, refreshed every frame; (3) the chevron warms to gold whenever the faced cell's occupant has `interact()` - NPC, door, chest, lever, crystal - making "will E work here" visible at a glance. Player-only by design; enemy/NPC facing carries no gameplay meaning. Placeholder-body path keeps its old face marker; the chevron is only built for sprite bodies. | Windowed: walk up to the forest NPC - chevron should point at them and turn gold; turn in place with taps - chevron snaps to all four edges; face a door/chest/crystal - gold; face empty grass - faint white | partial - gdtoolkit parse PASS on `player.gd`/`grid_actor.gd`; no Godot in the audit environment, so the RUNBOOK battery and the visual judgment (chevron size/alpha/offset: `PIP_NEUTRAL`, `PIP_INTERACT`, `PIP_EDGE_OFFSET` are the tuning knobs) still need Kayden's windowed pass | updated (`TASKBOARD.md` B-08 row + this proof row; code comments) | Tune chevron constants to taste in windowed play; verify the Kenney hero's native horizontal orientation (flip comparison may need inverting); directional frames at T-053/T-055 retire the chevron |
| 2026-07-10 | World Cohesion asset-lane planning | Codex | Added the parallel asset-only T-080..T-084 lane for the new-chat handoff: directional Hero/ooze motion and aggro read, forest NPCs, interactable-object states, combat VFX plus distinct slime variants, and final cohesion QA/manifest. Locked the lane to images, preparation scripts, provenance, manifests, and previews—no production GDScript, gameplay/data `.tres`, LDtk, imported levels, or combat UI edits—so it can run beside the PC gameplay/combat branch with a small later wiring diff. Every task names exact output families, shared 128px/four-slot/real-alpha/bottom-center contracts where applicable, and concrete visual/automated proof. | Read `TASKBOARD.md` -> Parallel Asset-Only Lane; it is the under-one-minute handoff artifact for the next chat. | pass - docs-only scope; `git diff --check`; task IDs checked unique through T-084 | updated (`TASKBOARD.md`); `BLUEPRINT.md`, `RUNBOOK.md`, `README.md`, and asset docs checked, no update needed because this commit plans work but changes no architecture, procedure, public behavior, or generated asset yet | Start T-080 in the new chat on `codex/asset-world-cohesion-pass`; do not wire production resources until the PC branch lands. |
| 2026-07-11 | Kenney-first visual-skeleton pivot | Kayden + Codex | Superseded the unstarted custom-generation T-080..T-084 lane with a staged Kenney runtime-adoption plan: promotion manifest, complete world/interactable skeleton first, actor skeleton, combat/UI/input prompts, then cohesion QA and documented custom-art swap points. The six CC0 source packs and existing generated art are retained; runtime scenes consume promoted copies rather than the source-library paths. | Read `docs/assets/KENNEY_IMPLEMENTATION_PLAN.md`; the delivery order and first visible milestone are summarized in `TASKBOARD.md` -> Parallel Kenney-First Visual Skeleton Lane. | pass - planning/docs only; source pack previews and 16x16 sheet metadata inspected; `git diff --check`; task IDs T-080..T-084 remain unique | updated (`docs/assets/KENNEY_IMPLEMENTATION_PLAN.md`, `docs/assets/ASSET_PLAN.md`, `TASKBOARD.md`); `BLUEPRINT.md`, `RUNBOOK.md`, and `README.md` checked, no update needed because no runtime behavior, architecture, procedure, or public-facing build changed | Start T-080 from the latest `integration`; T-081 is the first windowed milestone and should replace the red-square/blank-world presentation before deeper actor/UI polish. |
| 2026-07-11 | T-080..T-084 Kenney-first visual skeleton | Codex | Added a 36-entry validated promotion manifest and deterministic crop/LDtk-strip generator; switched all four LDtk projects to the runtime strip; replaced world actors, NPCs, slimes, doors, chests, plate, block, lever, and crystal stand-ins; applied Kenney combat terrain, cursor, highlights, menu/dialogue/boot framing; and completed T-079's keyboard/controller glyph mode without changing InputMap bindings. Preserved all generated/prototype assets as unwired alternatives. | Open `docs/assets/previews/kenney_contact_sheet.png`, then the refreshed forest/dungeon/combat screenshots in `docs/screenshots/`; run the two windowed proof scenes documented in `RUNBOOK.md`. | pass - red manifest suite failed before assets existed; first green pass exposed and preserved the four-frame SpriteFrames contract; final Godot 4.7 import; unit **29 suites / 180 tests / 715 checks**; full slice smoke **134/134**; main boot with `SceneManager ready.`; 1280x720 contact sheet, five-room tour, and combat showcase rendered and visually inspected | updated (`TASKBOARD.md`, `RUNBOOK.md`, `README.md`, `docs/assets/ASSET_PLAN.md`, runtime screenshots); `BLUEPRINT.md` checked, no update needed because mechanics, scene contracts, data model, and locked decisions are unchanged | T-084 remains `needs-review` only for Kayden's visual acceptance; final directional animation, portraits, VFX, and custom biome art remain deferred by the implementation plan. |
| 2026-07-11 | Kenney branch merged for local playtest | Codex | Fetched `origin/codex/kenney-visual-skeleton` and merged it into local `fix/qa-audit-b08-b21`, preserving the QA fixes while adopting the Kenney world, actor, combat, UI, and prompt visuals. Resolved the three overlaps in the taskboard, combat HUD, and overworld enemy rendering; enabled the promoted Kenney player/enemy sprites now that the awaited asset branch is present. | Run the project from Godot 4.7 and play the forest-to-tutorial-dungeon slice; compare against `docs/assets/previews/kenney_contact_sheet.png`. | pass - Godot 4.7 import exit 0; unit **29 suites / 180 tests / 715 checks**; full slice smoke **134/134**; `main.tscn --quit-after 1` exit 0 with `SceneManager ready.` | updated (`TASKBOARD.md` proof); merged branch already updates `RUNBOOK.md`, `README.md`, and asset docs; `BLUEPRINT.md` checked, no update needed because no mechanics, architecture, or locked decisions changed | Windowed visual/readability and control-facing judgment remains Kayden's playtest; overworld sprites are intentionally idle-only pending directional-animation polish. |
| 2026-07-11 | T-072..T-075 authored arenas | Codex | Replaced production `_arena_from_room` terrain copying with D-018: seven editable 17x7 forest LDtk levels, `ArenaData` registry, deterministic 5/2/1 weighted bags with biome/tags/fixed overrides and persisted state, explicit 8-slot deployment zones, safe side orientation, shared 4v4 validator, gallery, and live-combat showcase. | `docs/screenshots/authored_arena_gallery.png` plus `docs/screenshots/authored_arena_combat.png`; run `scenes/dev/arena_gallery.tscn` / `authored_arena_showcase.tscn` | pass - Godot 4.7 import; unit **32 suites / 199 tests / 955 checks**; main boot; slice smoke **134/134 on 5/5**; both 1280x720 captures rendered and visually inspected | updated (`BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, `README.md`, arena plan, runtime docs/screenshots); `AGENTS.md`, `CLAUDE.md`, and `docs/WORLD_LORE.md` checked; no update needed | Kayden must complete T-069 windowed acceptance; no claim of balance/feel acceptance yet. |
| 2026-07-11 | T-069 combat readability recut | Codex | Removed floating in-world unit text; placed party status, turn details, move prompts, and continue confirmation in bounded top/bottom HUD bands; and replaced the near-invisible move texture with filled blue tiles plus bright blue borders. The yellow cursor remains the exact selected destination. | `docs/screenshots/authored_arena_combat.png`; run `scenes/dev/authored_arena_showcase.tscn` | pass - Godot 4.7 import; unit **32 suites / 200 tests / 958 checks**; slice smoke **134/134**; 1280x720 windowed showcase rendered on NVIDIA and visually inspected | updated (`BLUEPRINT.md`, `TASKBOARD.md`, `RUNBOOK.md`, showcase screenshot); `README.md`, `AGENTS.md`, `CLAUDE.md`, and `docs/WORLD_LORE.md` checked; no update needed | Kayden still needs to confirm interactive readability, feel, and balance in the full T-069 windowed pass. |
| 2026-07-11 | T-088 | Claude (Cowork session) | **V2 vision pivot - docs-only canon reset.** Kayden's research pass (Horizon's Gate, Into the Breach, deterministic tactics) plus a design review he endorsed triggered a controlled reboot: one persistent world, one shared environmental vocabulary, visible party, deterministic telegraphed in-room encounters that permanently resolve problems, 3/4 perspective on the orthogonal grid, Steam-first. Rewrote `BLUEPRINT.md` (new identity/pitch/core loop, v2 pillar set, revised Non-Goals incl. the commercial flip, new **V2 Systems** section - shared vocabulary, party movement, deterministic combat contract with provisional intent rounds, perspective/elevation, roster targets, progression/persistence, story spine, reuse/rework/retire - plus supersession banners on the v1 sections and decisions **D-024..D-035** recording every superseded rule by ID: d10, D-005 single avatar, D-009 respawns, D-012/D-018 arenas, D-013 test companion, zoom transition, top-down contract, four-legendary-items spine, non-commercial non-goal). Updated `AGENTS.md` Locked Technical Decisions (surviving/new/superseded-do-not-rebuild). Reset this board: pivot sequence T-088..T-095 as the Ready lane, T-069 superseded, S-002/S-003/S-004 promoted to core, Backlog/Deferred re-triage notes, open D-027 + D-008-review rows in Pending Decisions. Also updated `README.md` (v2 pitch + pivot banner) and `RUNBOOK.md` (pivot note). **No code, scene, data, or test files touched.** | Read `BLUEPRINT.md` -> V2 Systems + Design Decisions D-024..D-035; diff the docs | pass (docs-only; grep consistency sweep run: no un-annotated d10/single-avatar/respawn/top-down claims remain outside superseded banners) | updated: `BLUEPRINT.md`, `AGENTS.md`, `TASKBOARD.md`, `README.md`, `RUNBOOK.md`; follow-up flagged: `docs/WORLD_LORE.md` realignment to D-035 | T-089 (3/4 visual spike) is next. D-027 turn structure open until T-092. D-008 defeat/XP flow review rides T-094. WORLD_LORE realignment not yet done. |
| 2026-07-11 | v1 WIP preservation | Claude (Cowork session) | Inherited ~3.2k lines of uncommitted v1 combat-readability follow-up (HUD recut, panel tile cursor, DungeonStoneHall arena + dungeon_guardian encounter, combat readability tour scene, test updates, T-069/readability screenshot sets) when resuming on `claude/unified-world-pivot`. Verified `--import` clean + unit suite PASS, then committed as-is (`8bf6758`) under the pivot's do-not-delete-v1 rule. `UI_ideas/` (Kayden's FF/Pokemon reference shots) left untracked deliberately. | n/a | pass | this row | none - preservation only, no new claims about the v1 work's acceptance |
| 2026-07-11 | T-089 | Claude (Cowork session) | **Pivot step 2 - three-quarter visual spike** on `claude/unified-world-pivot`. New throwaway dev scene `game/scenes/dev/three_quarter_spike.tscn` + `game/scripts/dev/three_quarter_spike.gd`: `SpikeRoom extends RoomGrid` adds integer cell elevation (D-030) with a `cell_to_pos` lift override so grid-snapped Tween movement rises/falls for free; 16x9 room with a +1 plateau, 4-tread stair ramp as the single cliff gap, 128px tall north wall face; shaded vertical faces vs. lit floors, lip/foot edge strips, contact shadows; leader is a real `GridActor` (Kenney hero frames), 3 breadcrumb followers are non-blocking (D-029), never in the occupancy map. High-ground bonuses/LOS/combat excluded per task scope. Verified: `--import` clean; scene boots headless (`THREE-QUARTER SPIKE: ready`); unit suite PASS (32 suites / 206 tests / 1024 checks); scripted windowed tours wrote 4 shots each at 1280x720 + 1920x1080, visually inspected (height reads via dark-face/lit-floor split; party reads as 4 distinct sprites; party spans both levels on the stairs in shot 03). | `docs/screenshots/t089-three-quarter-spike/{1280,1920}/` (4 PNGs each, <1 min flip-through); interactive: run `scenes/dev/three_quarter_spike.tscn` windowed, WASD | pass (agent-side); Kayden readability verdict pending | RUNBOOK spike-run section added; taskboard rows updated | Kayden's verdict on "does height read / does the party read as a party"; follower art is placeholder Kenney NPCs, not roster friends |
| 2026-07-11 | T-090 | Claude (Cowork session) | **Pivot step 3 - unified in-room encounter spike** on `claude/unified-world-pivot`, started ahead of the T-089 verdict (same room; logic survives a visual recut). New `unified_encounter_spike.{tscn,gd}` extending the T-089 spike: slime GridActor on the plateau; detection (Manhattan <= 2) or direct bump starts the encounter IN PLACE - no scene change, zoom, or camera move; followers snap onto their breadcrumb cells and enter the occupancy map (D-020 ally blocking in-encounter), friend fights via simple AI, deterministic damage max(1, atk-def) per D-026's first-cut formula (no rolls); victory fades the slime, vacates the followers (D-029 non-blocking restored), and exploration simply continues. Turn model is a throwaway step-tick; D-027 stays open for T-092. A PushableBlock is pushed pre-fight for the puzzle-state continuity proof. Verified: import clean; headless boot marker; unit suite PASS (32/206/1024); scripted windowed tours at 1280x720 + 1920x1080 pass all 8 continuity assertions (exit 0) and shots visually inspected. | `docs/screenshots/t090-unified-encounter/{1280,1920}/` (4 PNGs each); one command re-runs the 8-assertion tour (see RUNBOOK -> Unified in-room encounter spike) | pass (agent-side); Kayden play-check pending | RUNBOOK T-090 section added; taskboard rows updated | Kayden's played verdict; the step-tick turn model is deliberately NOT the D-027 answer |
| 2026-07-11 | T-092 | Claude (Cowork session) | **Pivot step 4 - deterministic intent prototype**, built to Kayden's same-day spec: the enemy keeps a rolling 3-verb plan (pure function of state, zero RNG anywhere), replanned when the current verb is impossible; FUTURE plan steps telegraph the verb only (not where/who), the CURRENT action declares full detail (locked cells, exact damage, exact status); player-side previews always show exact damage before commit. Round loop: enemy moves-or-declares -> player sees every target/effect (live re-previewed as units move) -> party acts in any order (Tab switch, move budget + one ability) -> enemy resolves against whoever REMAINS in the locked cells -> environment ticks (burn: exactly 1 dmg for exactly its duration; stun consumed at declare so "stun N" skips exactly N declares). Counterplay proven: dodge out, body-block the line, Bash stun-cancels, Shove push-cancels. Pure core in `scripts/dev/intent_logic.gd` + `tests/test_intent_logic.gd` (54 checks; red verified by a deliberate resolve mutation the suite caught, then restored); scene `scenes/dev/intent_prototype_spike.{tscn,gd}` on the T-089/T-090 room. Verified: import clean, headless boot, unit suite PASS (33 suites / 221 tests / 1078 checks), windowed tours at 1280x720 + 1920x1080 pass all 23 live assertions (exit 0), shots inspected. | `docs/screenshots/t092-intent-prototype/{1280,1920}/` (6 PNGs each); one command re-runs the 23-assertion tour (RUNBOOK -> Intent prototype) | pass (agent-side); **D-027 verdict is Kayden's after playing** | RUNBOOK T-092 section added; taskboard rows updated | Kayden's played D-027 verdict (intent rounds vs alternating initiative - the v1 combat path is still on disk if an A/B is wanted); obscure/freeze counterplay verbs and environmental reactions beyond burn wait for T-093 |
| 2026-07-11 | Unified-world branch consolidation | Codex | Fetched the live `codex/unified-world-pivot` (Sol) and `claude/unified-world-pivot` (Fable) branches, created `codex/unified-world-consolidation` from current `origin/integration`, and merged both histories. Resolved the shared-doc and unit-runner conflicts by keeping Fable's D-024..D-035 canon/T-088..T-095 queue, retaining Sol's isolated height and follower models as comparison harnesses, and registering all three new pure suites together. Added a critical keep/defer/owner-review record rather than prematurely generalizing either spike into production architecture. | `docs/UNIFIED_WORLD_CONSOLIDATION_REVIEW.md`; run the five dev scenes in `RUNBOOK.md` and compare the retained screenshots | pass - Godot 4.7 `--import` exit 0; unit **35 suites / 235 tests / 1,331 checks**; main boot exit 0 with `SceneManager ready.`; full slice smoke **134/134**; Sol height/party and Fable height/encounter/intent scenes all boot headless; existing screenshots visually inspected | updated (`TASKBOARD.md`, `RUNBOOK.md`, consolidation review); `AGENTS.md`, `BLUEPRINT.md`, and `README.md` resolved to the approved Fable canon; Sol's `WORLD_LORE.md` realignment retained; verification-only LDtk reimports restored | Kayden must decide perspective readability, party-motion density/leader switching, same-room combat feel, D-027 turn structure, and four-unit body-blocking before T-093 or production migration proceeds |
